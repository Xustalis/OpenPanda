# 更新履歴

[English](CHANGELOG.md) · [简体中文](CHANGELOG.zh-CN.md) · [日本語](CHANGELOG.ja.md) · [Español](CHANGELOG.es.md) · [Deutsch](CHANGELOG.de.md)

## ワンコマンドインストール

**macOS / Linux**

```sh
curl -fsSL https://raw.githubusercontent.com/Xustalis/OpenPanda/main/scripts/install.sh | sh
```

**Windows（PowerShell）**

```powershell
irm https://raw.githubusercontent.com/Xustalis/OpenPanda/main/scripts/install.ps1 | iex
```

**macOS（Homebrew）**

```sh
brew install Xustalis/openpanda/openpanda
```

インストール後は `panda init` でセットアップ、またはそのまま `panda` で REPL に入る。旧バージョンは同じコマンドでその場アップグレード（上書きインストール、ユーザーデータは保持される）。

## プロジェクト概要

OpenPanda（**Open** **P**ersonal **A**daptive **N**ode-based **D**istributed **A**ssistant）は個人向けタスクオーケストレーションカーネル：各デバイスで 1 つの `panda` バイナリが走り、ノード同士は認証付き WebSocket バスで発見し合い、エントリモデルが各リクエストを直接回答または実行可能なタスク仕様へ変換し、スケジューラが各タスクを最適なデバイスとエージェントへルーティングする。CLI がカーネルの主インターフェース（素の `panda` で対話 REPL に入る）で、Web コンソールは同じストアとエンジンの上に立つ薄いシェル。

## バージョン規則

- バージョンは `MAJOR.MINOR.PATCH` に従う。プロジェクトは初期開発期（`0.0.x`）：パッチリリースには機能追加・バグ修正・そして例外的な破壊的変更が含まれ得る——後者は必ず**破壊的変更**カテゴリに記載する。
- リリースは `vX.Y.Z` のタグで切る。前のタグ以降の全コミットが新バージョンのセクションに属し、`[Unreleased]` は直近リリース以降の作業を集める。
- 各バージョンは 4 分類で記録する：**追加**（新機能）、**修正**（バグ修正）、**変更**（改善と調整）、**破壊的変更**（アップグレード時に対応が必要）。
- 各項目は変更内容とユーザーから見える影響を 1〜3 行で書き、必要に応じて導入コミットを付記して遡りやすくする。
- 英語版（CHANGELOG.md）が正。zh-CN / ja / es / de の翻訳はそのミラーで、リリース前後は一時的に遅れることがある。

## [Unreleased]

## [0.0.9] - 2026-09-25

v0.0.9 安定版——コードネーム **Periapsis**。単体マシンでの利用では快適性のリリースです：承認は毎回の再回答ではなく記憶できるようになり、CLI はより親切に説明します（このノードが稼働中か答える status、次の一手を示す空のキュー、初回実行の案内、更新時に表示されるリリースノート）。Web コンソールは各回答を生成したエージェントとモデルに帰属表示し、ノードの稼働状態とバージョンをサイドバーに固定します。TUI は以前は崩壊していた日常の操作列にも耐えます。フリート向けには beta が開いた流れを完成させます：認証済みデータグラム面と NAT トラバーサル、暗号化 DTN ペイロード、スケジュールされたコンタクトウィンドウ、暗号的ノード同一性と署名済み同意、ステージ間の直接 artifact 受け渡し、制約デバイス向け lite ビルド。

### 追加

- **あらゆる面での承認記憶** —— tier-2 のプロンプトを毎回再回答する必要がなくなりました：決定はセッションまたはプロジェクト単位で記憶できます（一回限りが既定のまま）。エンジンはセッションスコープの決定を保持し、プロジェクト行は独自のポリシー（モード上書き + 既定スコープ + 記憶済み決定、マイグレーション V26）を持ち、記憶済みの*拒否*は再プロンプトではなく拒否でショートカットします。スコープは質問が出る場所で選べます：承認カードの once|session|project 行（1/2/3 キーとクリック可能セル）、`/approval` と `panda project approval` での実効ポリシー、`panda ask`/`session ask`、そして Web コンソールのキュー/セッション view のスコープピッカー——これらのビューは park された tier-2 タスクも表示します。
- **タスク活動ヒートマップ** —— 1 年分の日別タスク数をグリッドで描画：ウェルカムバナー、REPL の `/heatmap`、スクリプト向け `panda heatmap [--weeks N]`。NO_COLOR/ダム端末ではグリフランプ、非 Unicode コンソールでは ASCII セルに降格します。
- **`panda agents test <name>`** —— インストール済み agent CLI のワンショット診断：クレデンシャル来源、解決済みモデル、端点の到達性（状態詳細とレイテンシ付き）；`--json`・`--config` フラグ、利用不能なら非ゼロ終了。
- **実行帰属表示** —— 控えめな `agent (モデル) · ノード` の行が ask 出力（人間/JSON/ストリーム）、REPL タスクブロック、TUI ターン、パネルの approve 経路（永続化 ResultJSON から復元）を貫き、すべての回答が生成した実行者を名指しします。Web コンソールのセッション/詳細ビューにも同じ帰属が載り、システムのバージョンカードは `v0.0.9 Periapsis` を同じ重みで表示します。
- **Web サイドバーのノードチップ** —— コンソールのサイドバー下部に、このマシンの名前・稼働/停止ドット・実行中バージョンを表示し、System ビューへ一クリックで行けます；`GET /api/self` はリリースのコードネームも返します。
- **分散 DAG ランタイム** —— エンドツーエンドの Ed25519 ノード同一性：初回起動で永続鍵ペアを鋳造、署名付き hello（`PubKey`/`EdSig`。HMAC フォールバックで混在メッシュも継続動作し、ピア鍵は署名検証に通った場合のみ記録——マイグレーション V28）、そして署名済み tier-2 同意：起点ノードが `Authorized` を AuthSig/AuthPub/AuthTs で署名し、実行者は記録済み鍵で検証して偽造グラントを破棄（監査ログに `consent:forged`）；グラントはタスク行に永続化（マイグレーション V29）され、キュー転送や辞退リルートでも逐語再送出されます。ステージ出力は直接受け渡し：artifact フェッチは plan/consumer/producer/hash に束縛されたオーケストレータ署名グラントを携え、生産者はタスク行を持たない消費者にも供給できます；後続は即座に出力を adopt し、ハブ複製はバックグラウンドで引き取られ、生産者が届かない場合は入力フェッチがオーケストレータへフォールバックします。動的サブ DAG：`panda task add --parent-id/--preferred`、`panda_subtask_spawn`/`panda_subtask_await` MCP ツール、agent 環境へ注入される `PANDA_TASK_ID`。
- **DTN コンタクト計画** —— `network.contacts` がピア毎の予定送信ウィンドウ（[start, end) と任意の `period`・`rate_bps`）を宣言し、ハートビートと能力サマリで gossip され `employee_cache.contacts_json` に保存（マイグレーション V27）。`scheduler.ContactNextHop` はライブ辺と公告ウィンドウの和集合上で最早到着 Dijkstra を実行するため、計画を公開したオフラインノードも正当な custody 中継になります——ライブ位相ルーティングでは表せないケースです；転送が収まらないウィンドウと、バンドルの寿命後に到着する経路は枝刈りされます。どこにも計画がなければ従来のライブ位相探索にそのままフォールバックし、空の計画の公告はキャッシュ済み計画を消去します（旧ノードのフィールド欠落は従来通り温存）。
- **UDP データグラム面（farsky トラック）** —— `internal/bus/udp.go` が第 2 のトランスポートを追加：共有 UDP ソケット上の AES-256-GCM シール済みエンベロープと HMAC 署名パンチフレーム。メッシュ共有シークレットからドメイン分離で鍵導出し、1 データグラム 1400 バイト上限。`network.udp_listen` は既定で `listen_addr` のポートをワイルドカード面にバインド（全データグラムが認証済みのため、any-interface バインドでも LAN が既に見られる以上の露出はない）；`"off"` で完全に無効化。`sendTo` は引き続き WebSocket 接続を優先し、接続がない場合は確立済み UDP ルートへフォールバック。
- **メッシュ経由で調整する NAT ホールパンチング** —— `network.peers` が `punch:<node-id>` エントリを受理（ダイヤル不可能なピア向け）。`punch_offer`/`punch_ready` ペアはライブ接続経由、またはリンク状態グラフ沿いに中継され（TTL 制限、各ホップでエンベロープを再生成し起点はペイロードの `Src` に保持）、その後双方が交換した候補へ署名済みパンチフレームを散布し、ack がピンホールを確認するまで続く。確立したエンドポイントは UDP ルートになり、25 秒 keepalive で維持、失われれば再パンチ。
- **外部依存なしの反射アドレス発見** —— 同一 UDP ソケットが RFC 5389 binding リクエストに応答：公開アドレスを持つ任意のメッシュメンバーが艦隊の STUN サーバーになる（`internal/bus/stun.go`、依存ゼロ）；`network.stun_servers` は外部サーバーを運用・信頼するデプロイ向けのみ。hello 応答には `you`（リスナーが観測した送信元 IP）も載り、NAT 配下のノードは無料で自分の公開アドレスを得られる。
- **DTN バンドルペイロードの暗号化（v2）** —— `bus.Bundle.Seal` がメッシュ共有シークレットから派生したバンドルドメイン鍵で AES-256-GCM 暗号化し、署名ヘッダを関連データとしてバインド；`Open` は v2 を解封し v1 平文バンドルも検証するため、混在メッシュでも動作継続。中継 custody はシール済み blob のみを保存——中間ホップは読めない暗号文を転送する。
- **制約デバイス向け lite ビルド** —— `go build -tags lite` / `make build-lite-linux-{amd64,arm64,armv7}` は埋め込み Web コンソール（パネル端点は小型の案内ページを返す）と Bubble Tea TUI を外しつつ、daemon・メッシュ・DTN・キュー・ask・クラシック行 REPL を保持——Raspberry Pi や CLI 専用機向けに、フロントエンド依存のないより小さなバイナリ。`install.sh --lite` は `panda-<ver>-lite-<os>-<arch>` アーカイブを選び 32 ビット ARM も受け付け；デプロイスクリプトは SBC 級ターゲットで lite を既定にします。
- **リリースコードネーム** —— `internal/version.Codename` がこの系を "Periapsis" と命名；`panda version`、`panda --version`、`/api/version`、システムビューで表示。
- **ask エンジン向けキュー管理ツール群** —— `taskq_approve`（Tier-2：モデルが自分の通るゲートを自ら広げられないよう）、`taskq_reject`、`taskq_clear`（history/review/all の 3 スコープ）、`taskq_cancel` のバッチ `task_ids`；`taskq_list`/`taskq_show` は各タスクの承認ディスポジションを注記し、`taskq_priority`/`taskq_move` はキューにないタスクの暗黙の並べ替えを拒否。

### 変更

- **hello がデータグラム面を広告** —— `HelloPayload` が `udp_port` を載せ、応答は `you`（ピア観測の送信元 IP）を返す：リスナーは接続中ピアのパンチ候補を得、各ノードは設定なしで自分の公開アドレスを知る。
- **バンドルワイヤ形式 v3：相対ライフタイム** —— ワイヤ上のバンドルの絶対 `DeadlineUnix` が BPv7 流の（作成時刻, ライフタイム）の組に変わりました：各 custody 保持者が期限を自分の時計に再アンカーし、起点の時計を信頼しない——時計未同期のノード間で parked バンドルを正しく振る舞わせる鍵です。v1/v2 バンドルも引き続き検証可能：デコード済み生スロットは署名/AAD のために保存され、絶対期限は custody ロジック用にのみライフタイムへ正規化されます。
- **`panda init` がメッシュシークレットを播種し、モデルカタログを知る** —— 生成される設定は以前 `shared_secret` が空でした（WS リスナーは `pair`/`nodes add` が走るまで全ての内向ピアを拒否）；init は最初から鋳造します。対話式モデル選択は provider レジストリを列挙——`panda model add` や Web 設定ページと同じカタログ——であり、退役した既定値の固定 5 件リストではなくなりました；NoAuth プロバイダを尊重し（Ollama は鍵の質問をスキップ）、エンドポイントと共に provider/コンテキスト窓/max_tokens を書き込みます。`--defaults` では文書化済み `OPENPANDA_MODEL_*` 環境変数をファイルに取り込みます。
- **`panda doctor` がネットワーク面を検査** —— 空の `shared_secret` は失敗扱いになり（内向ピアリングを静かに無効化しつつデーモンは正常に見えた）、データグラム面はバインド先か意図的な `udp_listen=off` を報告します。
- **エージェントはディスパッチ前にプローブ** —— 解決済み端点への最小認証 POST が agent CLI のディスパッチ可否を決めます：確定的クレデンシャル系の 401/402/403/429 は拒否、5xx/トランスポート失敗は未到達、匿名 4xx は生存扱い。`AgentDispatchable` がマッチング・実行・能力広告をゲートするため、設定済みだが壊れた agent がプランを勝ち取って実行で落ちることがなくなりました。
- **データディレクトリより古いバイナリへの更新・インストールを拒否** —— `storage.LatestVersion` がバイナリのマイグレーション上限を公開し、`panda version --json` がそれを報告（到達可能ならデータディレクトリの生 `db_schema` も）；アップデータは差し替え前にステージ済みバイナリをプローブし、`panda install` は旧スキーマのコピーを拒否、`install.sh` もダウンロード済みアーカイブに同じ検査を行います——起動時に「schema version newer than binary」で死ぬようなダウングレードを事前に拒否します（意図的なダウングレードには `--force`/`OPENPANDA_FORCE=1`）。
- **日常 CLI の磨き上げ** —— `panda status` がノードディレクトリを正しく名指し（以前は「タスクがありません」と誤記）し、このノードが稼働中か・登録されたことがあるかを告げます；`panda queue` の空ボードは次の一手を提案；モデル未設定メッセージは yaml フィールドではなく `panda model add` や Web 設定を指します；設定ファイルが一つも無い初回実行でクラシック REPL が案内を表示；`panda update apply`/`upgrade` は入れたばかりのバージョンのリリースノートを表示；`init` の締めの行は `doctor`/`web`/`panda` を次の一手に挙げます。
- **統一されたバージョン表記** —— `v0.0.9 Periapsis` が `--version`/`version`、REPL ウェルカム、TUI スプラッシュ、lite 起動行で同じ重み・引用符なしで表示され、Web サイドバーのノードチップにも載ります。

### 修正

- **1 レスポンス内の全ツール呼び出しがそのラウンドで実行される** —— 以前はターンごとに最初の `tool_use` のみ実行し残りは注記に回したため、複数のキュー操作を出すモデルがツール予算を再送で使い果たしていた（報告された「タスクキューを掃除して」障害）。ネイティブ応答は全呼び出しを実行し、Anthropic プロトコル通り assistant `tool_use[]` 1 件 + user `tool_result[]` 1 件で再生；DSML invoke は順次実行し散文として再生。
- **DSML パーサが DeepSeek の実際の出力形式を受理** —— シングル/ダブルパイプの `<｜DSML｜>` バリアント、緩い引用の invoke 名、JSON 引数ボディでプロトコル文字列が返信に漏れなくなった。
- **承認状態が実際の意味を露出** —— `approval_disposition`（accept_work / resume_execution / needs_changed_input）が `taskq_list`/`taskq_show`、タスク API ペイロード、キュー/詳細ビューで可視化され、入力変更が必要なタスクでは承認を無効化；成果済みタスクと実行許可待ちタスクが区別不能ではなくなった。
- **resume_execution タスクへの `Approve` が再キュー可能に** —— resume 分岐は行を `scheduled=1` とし `ListReady` がクレームできる；直接の Approve が誰にも拾われない `queued` 孤児行を生まなくなった。
- **ツールループ枯渇時はダイジェストに降格** —— 使い切ったツール予算は「max tool rounds」の素のエラーではなく、実行済み呼び出しの要約を返す。
- **TUI のコマンド実行がフロントエンドを壊さなくなった** —— 日常の操作列（`/clear`、数ターン、打ち間違えたコマンド）で Bubble Tea を迂回して実端末へ書き込んだり、対にならない履歴を残したりしていました。`/clear` は `tea.ClearScreen` で再描画；exec ポンプはコマンドに空 stdin を与え、ハンドラの panic をエラーブロックに回復；捕捉出力はコミット時とライブ描画時に ANSI 除去；状態を変えるコマンド（`/new`、`/resume`、`/project`、`/authorize`、一覧の変更）は共有ヘルパー経由で Update ゴルーチン上で動き、状態の二重書き込みを解消；失敗したターンは user+error の対として永続化され、次の ask が 400 にならなくなりました；`/history` は永続化済み会話へフォールバックし、`/new` はメモリとファイルの両方を消去します。
- **アウトボックスがパンチ済みデータグラム経路で吐き出される** —— `sendableTo` は UDP ピンホール経由でしか届かないピアを送出不可と見なし、punch-only ピア宛の parked バンドルが存在しない WebSocket 接続を永遠に待っていました；UDP ルート経由で排空されるようになりました。
- **DTN 中継の計上が再起動を越える** —— バンドル毎の中継ホップ予算がメモリではなく `dtn_relay_log`（マイグレーション V25）に永続化され、`parkBundle` の upsert が `via` を更新するため、再配送バンドルの no-echo 規則が最新の内向き経路に従う。
- **スキル URL インポートが内部ネットワークを探れなくなった** —— `ImportURL`（`panda skill install <url>`、`panda_skill_install`、`skill_install`、`/api/skills/import` の背後の経路）は非ループバックのホストに https を必須とし、接続時に解決済み IP をすべて検証します：ループバック、RFC1918/ULA、リンクローカル（クラウドメタデータ端点）、マルチキャスト、未指定アドレスは一切ダイヤルせず、リダイレクトも同じ分類を満たす必要があるため、agent やインデックス由来の URL がノードを SSRF 踏み台に変えられません（強化：`internal/skills/fetchguard.go`）。
- **STUN binding 応答を送信元 IP 毎にレート制限** —— 1 ソース毎秒 10 応答と有界追跡マップで、UDP ソケットの約 2 倍の反射係数を、実クライアントを害さずに抑制します（RFC 5389 の再送は秒オーダー）。

### 破壊的変更

- **`/api/*` の `?token=` クエリパラメータ認証を廃止** —— パネル API は `Authorization: Bearer <token>` のみを受け付けます。URL 内のクレデンシャルはブラウザ履歴やプロキシの access ログに残るためです。同梱コンソールは SSE ストリームを含め全てでヘッダを送り（EventSource ではなく fetch ベース）、`panda web` が開く `/?token=` 自動ログイン URL は影響を受けません——それは静的ページであって API ではありません。外部スクリプトとサードパーティクライアントはヘッダへ移行してください。
- **ビルトインのモデルベンダー既定値を廃止** —— `model:` が空の設定はかつて「黙ってバンドル済みベンダー端点を使う」を意味し、生成された設定に書き込まれ、バナーでユーザーの選択のように見せ、未設定ノードのプロンプトをそのベンダーへ流していました。`entry.NewClient` は `ErrNoModel` を返し、`panda ask`/`voice`/REPL/TUI は設定済みモデルを要求して `panda init`/`panda model add` へ案内します；`config.example.yaml` は当該セクションをコメントアウトして説明を添え、クレデンシャル注入（`injection.model`）も実在の端点を要求します。暗黙の既定に依存していたデプロイは `model.provider`/`model.base_url` を設定するか `panda init` を実行してください。

## [0.0.9-beta] - 2026-09-23

v0.0.9 beta はハイブリッド転送/DTN アーキテクチャを完成させました：メッシュルーティングは遅延加重になり、DTN バンドルはオンラインのリンク上を実際に移動しオフラインのリンクでは待機（park）され、委任トークン予算は実際の計量で強制され、プリエンプトされた作業はシャドウコピーで存続し、ハードウェアアクチュエータに実行経路が加わりました。さらに、委任されたエージェントにノード自身のツール群への管理された経路を提供し、モデル設定面を完全に実装し、モデル管理 TUI を全幅レジストリと単一画面フォームエディタで再構築しました。

### 追加

- **遅延加重メッシュルーティング** —— 近隣ノードがハートビートでリンクごとの RTT サンプルを gossip し（`HeartbeatPayload.Links`、`employee_cache.links_json` に永続化）、`graphFirstHop` はホップ数 BFS ではなく実測リンクコスト上の Dijkstra を実行します。未測定のエッジは `unknownLinkCost` で計算されるため、実証済みのマルチホップ経路が高コストの直リンクを上回ることがあります（§4.1）。
- **ワイヤ上の DTN バンドル** —— park された委任タスクは署名付き CBOR バンドル（HMAC-SHA256、EID、TTL）に包まれ `task_outbox.payload_blob` に格納され、`dtn_bundle` エンベロープでバイト単位そのまま転送されます。受信側は完全性と期限を検証し、宛先 EID に届けるか、次のホップのために転送/park します（§8.3、§9.1）。
- **定期的な outbox スイープ** —— `sweepOutboxes` は peer hello 時だけでなくタイマーで result/cancel/task の各 outbox をリトライし、ピア単位の flush クレームにより並行 flush がインターリーブしないようにします。期限切れバンドルはクリーンに破棄され、タスクは expired とマークされます。
- **メッシュ全体のトークン予算** —— マイグレーション V20 が `tasks.token_budget` を追加（0 = 無制限/レガシー、正 = 残りクォータ、-1 = 枯渇センチネル）。`SpendTokens` がアトミックに減算し、エージェントと supervisor/judge の使用量はモデル呼び出しごとに課金され、枯渇したタスクは死んだクォータを消費し続ける代わりに review に収束します（§6.1）。
- **yield 時のシャドウ作業コピー** —— yield を求められたタスクはキャンセル前に宣言スコープを `.panda-shadow` にスナップショットし、再開タスクは競合しないファイルをマージして戻します（競合は勝者のバイトを保持し、システムノートで報告）。従来の全損キャンセルを置き換えます（§5.2）。
- **アクチュエータ実行経路** —— ケイパビリティカードのアクチュエータが `command`/`args`/`tier` を宣言でき、commander は一致するタスクをネイティブエグゼキュータ経由で `ActionSpec` プレースホルダ置換付きでルーティングします。`panda card invoke <actuator-id> [action] [name=value ...]` で `--dry-run`/`--authorize` 付きのローカルテストが可能です（§7）。
- **エージェント自己管理面（`panda mcp`）** —— ノードは独自の MCP stdio サーバーを同梱し、OpenPanda の管理面を第一級のエージェントツールとして公開します：`panda_status`、`panda_nodes`、`panda_queue`、`panda_task_status`、`panda_task_submit`（`panda task add` へのシェルアウト、セッションあたり最大 8 件）、`panda_skill_list`、`panda_skill_install`（hub 名または http(s) URL）、`panda_skill_import`（生の SKILL.md）、`panda_card`。`routing.tools_policy: extended` では、commander が作業ディレクトリの `.mcp.json` に、設定済みの `mcp.command` パススルーの隣へ `openpanda` エントリを実体化するため、MCP 対応エージェント CLI が自動で発見します。シェル実行可能なエージェントには PATH 上の `panda` バイナリを指すプロンプトヒントが渡ります。`routing.panda_tools: false` で拡張ツール面を維持したまま自己管理面をオプトアウトできます。
- **チャンク分割・再開可能な artifact プッシュ（`artifact_push`）** —— インライン上限を超える大きなバンドルペイロードは pull 経路に任せず 1 MiB フレームでストリーム送信されます。マイグレーション V21 の `artifact_push_outbox` が送信側のカストディを再起動を跨いで永続化し、受信側は `<pool>/.staging/` 配下にチャンクをステージして連続したウォーターラインを報告します。再接続時は再送ではなく確認済みオフセットから再開します。入力が未着の dtn タスクは失敗せず `waiting_context` に park され、転送したリレーは artifact が到着し次第先へプッシュします（チェーンカストディ、§8.3）。
- **無制限の artifact 転送** —— 512 MiB の `artifact.MaxBytes` 上限を撤廃：プールは既定で無制限、ファイルシステムの空き容量が上限です。ボリュームが収容できない `total` の宣言は最初のバイトが届く前に拒否されます（`ErrNoSpace`）。`storage.artifact_max_bytes` / `OPENPANDA_ARTIFACT_MAX_BYTES` で小容量ディスクのノードに固定上限を再設定できます。
- **マルチボリューム artifact プール** —— `storage.artifact_extra_paths` / `OPENPANDA_ARTIFACT_EXTRA_PATHS` がプールにボリュームを追加：書き込みは使用可能容量が最大のボリュームを選び（`free − storage.artifact_min_free_bytes`、既定 256 MiB の余裕。明示的な 0 でウォーターマーク無効）、読み取りは全ルートを検索し、ステージ済みチャンクは選択したボリュームに置かれるためコミットの rename がアトミックのまま保たれます。追加パスは容量を増やすだけで artifact を複製しません。入站データの置き場は受信ノードだけが決めます。
- **DTN マルチホップリレー** —— `relayBundle` は宛先への直リンクを待ちません。`scheduler.DTNNextHop` が gossip トポロジー上で宛先対応の加重経路を走らせ（中間ホップはオンライン必須、宛先はオフライン可）、署名済みバンドルをそのままより近い接続済み次ホップへ転送し、受信ピアへエコーバックしません（`task_outbox.via`、マイグレーション V22）。outbox flush のたびに次ホップを再計算します。バンドル単位のリレーログが転送を `dtnRelayMaxHops` に制限し、バンドル期限とともに失効します。
- **エージェントセッションのチェックポイント** —— マイグレーション V23 が `tasks.agent_session_id`/`agent_session_node` を追加：アダプタの会話ハンドルは各ラウンド後に永続化され（`context.WithoutCancel` のため yield キャンセルでも失われない）、result ワイヤで委任元に報告され（`agent_session_id`）、再ディスパッチ時には生成したノードにのみ `resume_session_id` として提供されます。中断したタスクは冷起動ではなくエージェント自身の会話を再開します。セッション非対応のアダプタはフィールドを空のままにし、シャドウコピーのフォールバックを維持します（§5.2）。

### 変更

- **ハートビート gossip が隣接関係とリンクメトリクスを運搬** —— `Core.broadcastHeartbeat` が稼働中の近隣と実測 RTT を配信し、`handleHeartbeat` がそれらをディレクトリに折り込むため、ルーティング決定は RTT サンプル到着のたびに更新される実際のリンクコストを反映します。

### 修正

- **Windows のクラシックコンソールが生のエスケープシーケンスではなく色を正しく表示** —— パレットは Windows の TTY では常に SGR コードを出力していましたが、従来型コンソールホスト（cmd.exe、Windows PowerShell 5.1、ダブルクリック起動のバイナリ）はプロセスが VT 処理を有効化するまでそれを解釈しません。`internal/cliui` は初回使用時に stdout の `ENABLE_VIRTUAL_TERMINAL_PROCESSING` を有効化し、すべての ANSI 出力をそれにゲートするため、VT を有効化できない環境では `[31m` の文字化けではなくきれいなプレーンテキストを出力します。
- **Windows のクラシック REPL に本物のラインエディタを搭載** —— 従来は裸のスキャナ読み取りで、カーソル移動・履歴・Tab 補完がなく、実行中の ask で Ctrl-C を押すとコンソール制御ハンドラに当たり REPL 全体が終了していました。新しいコンソール API ベースのエディタにより、行内編集、永続化履歴（`%LOCALAPPDATA%\openpanda` に保存）、スラッシュコマンドと引数位置の補完、実行中 ask の Esc/Ctrl-C キャンセル（二度押しで終了、unix と同様）、および実際の端末幅の取得が可能になります。
- **`panda uninstall` が既定位置の設定と能力カードを削除** —— ユーザー既定ディレクトリ（例 `%APPDATA%\openpanda\config.yaml`）にある設定は「custom location — kept」と判定され、API キーや共有シークレットがディスクに残っていました。能力カードは計画に含まれることすらありませんでした。どちらも標準のユーザー/システム設定ディレクトリ内にある場合は削除対象になります（Linux の `~/.config/openpanda` も同様に修正）。
- **Windows のブラウザ起動が URL の特殊文字で壊れない** —— `cmd /c start <url>` は `&`・`^`・括弧をシェル構文として解釈していました。`panda web` はトークン付き URL を `rundll32 url.dll,FileProtocolHandler` で開くようになりました。
- **Windows のインストール/アンインストールの堅牢性強化** —— インストーラーの自己チェックはゼロ以外の終了コードで失敗するようになり、壊れたバイナリでも成功と報告されることはなくなりました。PATH 比較は `%VAR%` エントリを展開して重複追加を防止。`taskkill` によるキャンセルでコンソールウィンドウが表示されなくなり、ハードウェアプローブは `pwsh` を優先して `powershell` にフォールバック。`ProgramData` 未設定時は `SystemConfigDir` が `C:\ProgramData` にフォールバックし、REPL 履歴は unix 風の `~/.local/state` から `%LOCALAPPDATA%\openpanda` に移動しました。

## [0.0.8] - 2026-09-20

公式 v0.0.8 リリース：OpenPanda はプレビューから安定版の公式ベースラインへと移行し、プロンプト多言語ポリシーとエンドツーエンドの多言語ローカライズ、プロバイダー地理分類、認証情報の自動サニタイズ、SQLite WAL 並行性最適化、権限階層の下限強化、インストーラーと端末対話の修正を包括的に導入します。

### 追加

- **プロンプト多言語ポリシーと出力ローカライズの分離** —— `internal/core` で `PromptLanguagePolicy` を実装し、元の指示の純粋性を保ちながら、ユーザーの言語要件をエージェントおよび実行プロンプトにコンテキスト伝播付きで注入します（b4ef8a6）。
- **エンジンワークフローのローカライズと CLI 言語セレクター** —— デバイス概要、高速トリアージ、タスク送信、サブエージェント進行状況メモ、および結果要約が、中国語、英語、日本語、スペイン語、ドイツ語に対応。`panda ask`、REPL、Bubble Tea TUI で `--lang` フラグからアクセス可能になりました（6ac309d、fc96d8c、8d6ce28）。
- **プロバイダー地理分類** —— `internal/providers` にプロバイダーの発信元分類と `DetectRegion` 推論を追加し、ホスト地域とコンプライアンス要件に基づいてタスクを動的にルーティングします（35443bc）。
- **認証情報の自動サニタイズ** —— `internal/core/sanitize.go` がプロジェクトのパッケージング、タスクペイロード、実行ログから API キー、秘密鍵、トークンを自動でスキャンし置換（redact）します（d91a4bf）。

### 修正

- **SQLite WAL の並行性とロック調整** —— SQLite 接続プラグマ（`journal_mode=WAL`、`synchronous=NORMAL`、`busy_timeout=5000`）を最適化し、高い並行ノードトラフィック下でのライターの枯渇と競合を解消しました（d91a4bf）。
- **ケイパビリティ階層の下限強化** —— 防御権限の評価を厳格化し、影響の大きい不可逆な操作が確実に手動承認キューに入るようにしました（d91a4bf）。
- **ヘッドレスな Linux ホストでもインストールが中断されなくなった** —— `scripts/install.sh --yes` は `$USER` を参照していたが、コンテナ・CI ランナー・素の SSH セッションではこの変数が通常存在しない（まさに `--yes` が必要とされるホスト）。`set -u` の下では、バイナリ・PATH エントリ・セルフチェックがすべて成功した直後にスクリプトが中断し、成功したインストールが失敗として報告され、完了メッセージも出なかった。ユーザー名は自前で解決するようにした。
- **Windows のアンインストールでログオンタスクが削除される** —— `panda uninstall` が停止していたサービス名は、自動起動がスケジュールタスクに移ってからインストーラーが作成しなくなっていたため、`OpenPandaNode` はアンインストール後も残り、削除されたばかりのバイナリを毎回のログオンで起動しようとして、すでに製品を削除した利用者にエラーを報告していた。タスクを削除するようにした。
- **存在しないリリースに固定されていた Homebrew フォーミュラ** —— tap のフォーミュラは `v0.0.8` を指していたが、そのタグとリリースは削除済みで、`brew install Xustalis/openpanda/openpanda` は 404 のアーカイブを取得していた。tap は最新の公開タグに戻し、公開アセットとバージョン・SHA-256 を照合するガードを追加した。
- **TUI：素早いスクロールでマウス座標が入力欄に打ち込まれなくなった** —— トラックパッドの一振りは数十のホイールノッチに及び、Bubble Tea の 256 バイト入力バッファを溢れさせてイベントの途中で切れる。X10 の座標にはエスケープ前置きが付かないため、前置きが消費された後の残りバイトは手入力と区別できず、`[-` や `13M` がそのまま入力欄に入っていた。切り詰められた前置きは今も通知されるので、そこから「あと何バイト残っているか」を求め、その分だけを飲み込む。ガードが見るのはメッセージの形だけで中身は読まない（読み込みバッファはメッセージが渡された瞬間に次の読み込みで上書きされる）。また武装するのはアプリがマウスを保持している間だけなので、既定の select モードでキー入力を食うことはない。

## [0.0.8-preview] - 2026-09-19

### 追加

- **ケイパビリティカードの自動検出とワンストップ設定** —— ホストのプロービング（ハードウェア、インストール済みエージェント CLI、リソースクラス）が共有パッケージ `internal/carddetect` に集約され、`panda detect`、`panda card rescan/edit`、オンボーディング、Web パネルのすべてがこれを利用します。カードを持たないノードは、初回利用時に失敗する代わりにカードが自動で組み立てられます。
- **スケジューラの遅延初期化** —— Ask エンジンは起動時に `CardPath` を要求しなくなり、カードの場所は設定または既定パスから導出されます。スケジューラは最初のタスク送出またはカード変更時に立ち上がるため、カードがまだ存在しなくても回答とメモリツールが機能します。
- **あらゆるサーフェスからの構造化カード編集** —— エージェント項目にフィールド単位の `card_agent_set` 更新が加わり、Web パネル、REPL/TUI、CLI のすべてが共有の検証付きパス（`internal/cardmut`）を通じて `capabilities.yaml` を変更します。手書きのコメントはそのまま保たれます。
- **厳選オフラインカタログ付き Skills Hub** —— `panda skill hub list|search|info|install`（REPL では `/skill hub …`）で本番品質のスキルを閲覧・インストールできます。最初の内蔵プレイブック一式はバイナリに同梱され、初回使用時に初期化されるため、新規ノードが空のまま始まることはありません（668ddcd）。
- **パス・URL・アーカイブからのスキルインポート** —— `panda skill import` はローカルパスまたは URL、単一の markdown ファイルまたは tar.gz/zip アーカイブを受け付け、scope/name/status/force の上書きに対応。ダウンロードは 10 MiB に制限され、frontmatter の検証を通過したものだけが書き込まれます（668ddcd）。
- **エージェントツールとしての自律的スキル発見とインストール** —— Ask エンジンはタスクの実行中にハブを検索しスキルをインストールできます。ワークフローを必要とする作業は、それを欠いて失敗するのではなく、自分で装備します（a5fa093）。
- **`panda skill` コマンドファミリーと `/skill` REPL コマンド** —— `list`・`approve`・`reject`・`reset`・`find`/`discover`・`import`・`hub`・`install`/`add` を提供し、スクリプト向けに JSON 出力も可能（a5fa093、f5b6884）。
- **Web コンソールのスキル管理** —— スキルパネルでインストール済みスキルの閲覧、ハブ検索、ブラウザからのインストールや有効化・無効化ができ、CLI と同じストアが背後で動きます（35e282b、f5b6884）。
- **プロジェクトが第一級市民に** — projects テーブル（作業ディレクトリ、説明、タイムスタンプ）と、ワンショット・プロセスでも生き残る settings 支援の現在プロジェクト・ポインタ（`panda ask` は常駐しない）；完全な CLI 面 `panda project list | new --dir --desc | show | rename | remove | enter | exit`、`list` は現在のものをマークする（a9fa471, 1260cb9）。
- **タスクは入室したプロジェクトを継承** — エンジンが環境プロジェクト（ambient project）を運び、分類器が指定しなかった部分を補う。プロジェクト内のタスクは自分の作業ディレクトリと説明を知る——以前は、どのプロジェクトにも属さないタスクより知っていることが少なかった（8c82c5e）。
- **委譲タスクはプロジェクトを同伴する** — プロジェクト記憶は委譲ペイロードに詰め込まれ（サイズ上限あり）、作業ツリーは plan 面の機構を再利用したチャンク化 artifact 参照として運ばれ、実行側は自分のルート下で作業ディレクトリを再導出し、パス文字を含むプロジェクト名は拒否される（バス由来の名前は信頼できない入力）。完成した成果物は上書き式でローカルのプロジェクト・ディレクトリへ引き取られる——2 台が1 つのプロジェクトを編集するのは実際の競合であり、黙ってマージされることはない（a1f1d19）。
- **コンソール：プロジェクトと設定** — プロジェクト CRUD、入室/退室、メタデータがコンソール API へ（7cda886）；作業ディレクトリと現在プロジェクト状態とその変更操作を載せたプロジェクト行（146c1ba）；そして承認・ルーティング・記憶上限・注入設定——承認ゲートは、キューを一午後見た後にユーザーが最も変えたくなる設定であり、もうコンソールを出る必要がない。4 項目すべてコンソール既存の settings API に合流し、第二のエンドポイントは作らない（7a60a80, 0736c2f）。
- **Plan ボード・エンドポイント** — `GET /api/plans`（どんな plan があり、どこまで進んでいるか）と `GET /api/plans/{id}`（1 つの plan の各ステージとステージ間の artifact 配線——「訓練ステージは本当にスクリプトを受け取ったか」に答えるビュー）。plan の開始は、既に動いていた `/api/ask` のまま（932442a）。
- **管理ツールファミリー（Management Tools）** —— Ask エンジンが第 1 層の内部ツール（`system_status`、`card_list`、`card_show`、キュー照会）を直接呼び出し、クラスタの状態や各デバイスの能力をリアルタイムに回答可能に。
- **TUI 途中ステアリングと即時停止** —— 実行中のタスクのキャンセルやステアリング、マウス操作、タスク進捗の可視化に対応。
- **アクティブ・フェイルオーバーと認証救済モデル注入** —— エージェントのクォータ切れや認証失敗時に、設定済みフォールバックモデルを自動注入。
- **実行進捗とモデル割り当ての透明化** —— Agent の実行コマンドやツール呼び出しを `EvProgress` でリアルタイム同期表示。
- **マルチモデルレジストリ（`/model`）** —— エントリモデルは単一スロットではなくなりました。`models:` リストが名前付きモデルを保持し、`/model <alias>` で切り替えられ、`/model add`・`/model list`・`/model fetch`・`/model test` が DeepSeek、Claude、ChatGPT、Kimi、Volcengine Ark、Zhipu、Qwen、SiliconFlow、OpenRouter、Ollama に対応します（08ede13）。スマートな base_url 正規化（末尾スラッシュ、欠けたバージョン接頭辞、プロバイダごとの癖）、思考モード・モデル向け reasoning フィールド、OpenAI プロバイダ・プリセットも含みます。
- **スラッシュメニューの引数候補** —— 列挙型の引数を持つコマンド（`/lang`、`/resume`、`/config set` など）は、コマンド名の後にスペースを入力するとコマンド一覧と同じ矢印キーメニューが開く。
- **承認カードの矢印キー選択** —— 第 2 層承認プロンプトが ↑↓/←→ と Enter でも回答できるようになった。
- **フルスクリーン TUI と初回起動オンボーディング** —— ターミナル UI はキーボードナビゲーション付きのオルタネートスクリーンに移行し、初回起動ウィザードが新しいインストールを最初のプロンプトまで案内します（ef82d6f）。
- **モデル健全性サーキットブレーカーとフォールバックルーティング** —— 繰り返し失敗するモデルはクールダウンに入り、エンジンは死んだエンドポイントを叩き続ける代わりに健全なフォールバックへ作業を再ルーティングします（7c5c643）。
- **Web コンソールのセッションキャンセル** —— 実行中のセッションをブラウザから停止でき、キャンセルは他のコントロールと同じコマンド経路を通ります（35e282b）。
- **コンソール向け keepalive リトライクライアント** —— Web クライアントは切断された接続をバックオフ付きで再試行し、一時的なネットワーク障害で UI が死んだストリームに取り残されることはありません（35e282b）。
- **プロキシ対応サンドボックス** —— 実行サンドボックスが標準のプロキシ環境変数（大文字・小文字の両方）を子プロセスへ引き渡すようになり、プロキシ経由のインストールやダウンロードが動作します。
- **エージェント検出範囲の拡大** —— レジストリがより多くのバイナリ別名（`claude-code`、`grok-build`、`deepseek-harness`、`hermes-agent`）を認識し、`PATH` に加えて一般的なインストール先（`~/.local/bin`、`/opt/homebrew/bin` など）も探索します。OpenCode の認証情報検出は現在の設定レイアウトに追随します。
- **noauth モデル対応** —— レジストリのエントリはエンドポイントが `Authorization` ヘッダーを受け付けないことを宣言でき、それを拒否するローカルサーバーをカバーします（7c5c643）。
- **タスクキューのクリアと削除** —— CLI・API・Web コンソールから同じ操作ができます（d5df3d3）。
- **コンソール：ディレクトリピッカーと実行イベントタイムライン** —— 作業ディレクトリは手打ちではなくブラウザのダイアログで選択。エージェントのコマンド、ファイル編集、ツール呼び出しがセッション詳細ビューに時系列のタイムラインとして描画されます（291dc76）。
- **`panda read` コマンド** —— markdown 文書とテキストをシンタックスハイライト付きで端末に直接表示し、パイプ出力もそのまま保ちます。

### 変更

- **TUI がマウスをターミナルに返上** —— フルスクリーン UI は既定でマウスを捕捉しなくなり、ドラッグ選択、ダブルクリック、ターミナル自身のコピーショートカットが再び機能します。スクロールは失われません。オルタネートスクロールがホイール 1 ノッチを矢印キーに変換し、PageUp/PageDown は常に有効、`ctrl+t` で旧来の捕捉モードへその場で切り替わります（`ui.mouse: scroll` / `PANDA_MOUSE=scroll` で永続化）。どちらのモードでも知っておく価値があるのは、捕捉がコピー自体を奪っていたわけではなく、修飾キーなしのコピーだけを奪っていたという点です。iTerm2 なら `Option` を押しながら、Terminal.app なら `Fn` を押しながら選択すればコピーできます。
- **承認ゲートは不可逆作業のみをカバー** — Tier 2 は今や「後続のコマンドでは戻せない」を意味する：削除、ディスク/パーティション/ファームウェア状態、電源状態、権限昇格、そして作業を失う引数形式（`git push --force`、`rsync --delete`、`sed -i`、`find -delete`）。curl、wget、make、ssh、systemctl、mount、docker、kubectl、terraform、各パッケージマネージャ、chmod/chown/mv/cp/tee などは承認なしで走り、`bash scripts/build.sh` も確認を求めない——自分の build を走らせられないノードは、存在意義を果たせない（e593470）。
- **ファイルへ保存する取得は引き続きゲート** — バイトをパスへ保存する curl/wget（`-o`、`-O`、`--output`）は Tier 2：バイトは分類器から見えず、次のステップは大抵それを実行することであり、この変更前は `curl -o x …; bash x` が終始 Tier 1 と判定されていた。stdout や `/dev/null` への取得——到達性プローブの書き方——には影響しない（2026-09-02 レビュー P1）。
- **システムプロンプトを英語で書き直し、タスクルーティングを強化** —— エントリモデルは実行やエージェント派遣の要求に対し、受動的に雑談するのではなく即座にタスク JSON を出力することが明示的に求められるようになりました。派遣関連のトリガー語（`调度`、`派发`、`dispatch`、`schedule` など）は、プロンプトが用いるすべての言語で認識されます。
- **スラッシュコマンドとシェルコマンドが TUI 内に留まる** —— `/…` と `!…` の行はターミナルを解放しなくなり、出力はスクロールバックへ直接取り込まれます。チャットビューには PgUp/PgDn とマウスホイールのスクロールが加わり、後から見返せます。
- **プロバイダ障害処理の高速化と高精度化** —— アダプタは認証・クォータ・5xx・プロバイダ障害を決定論的な終了コードで報告し、司令塔はアダプタごとにエージェント名とエンドポイント/モデルの組を正規化します（DeepSeek の二重エンドポイントを含む）。タスクが特定要件を指名しない場合は汎用エージェントへフォールバックするため、注入に失敗しても死んだエンドポイントを再試行せず復帰します。
- **どこでも同じウェルカムバナー、より洗練された REPL 操作感** —— ベア REPL・ヘルプ・エラー経路が同じ ASCII ロゴの挨拶を共有し、TUI ビュー層も合わせて磨かれました（0d44ee4）。
- **TUI：視覚ブロックの分離と高コントラストのプロンプトパネル** —— ユーザー入力・アシスタント出力・システム通知が明確に分離されたブロックを占め、ユーザープロンプトはどのテーマでも際立ちます（b0979cc）。
- **コンソール：「Panda Paper」ビジュアル刷新とワークフロー磨き上げ** —— ウォームペーパーのライトテーマとウォームインクのダークテーマ（純粋な黒もコールドグレーも使わない）、竹緑のブランドアクセントを一段落ち着かせ、青は意思決定オービット経路専用に、見出し層のみにセリフのディスプレイ書体、角丸を一段大きく、より浅く柔らかいウォームブラウンの影、「AI 感」装飾（見出しのグラデーション文字、グラデーション下線、ボタンのハイライト覆層）を除去。セッションビューとプロジェクトビューもワークフローアーキテクチャに合わせて作り直し、アクション配置と状態表示が一貫しました（291dc76）。この層は同名トークンの再定義とカスケードで効き、コンポーネントの構造・クラス名・ロジックは無変更です。

### 修正

- **TUI：ターミナル入力のエスケープシーケンス残渣漏れ** —— 読み取り境界をまたいで分断された、あるいは未解析の ANSI エスケープシーケンス（SGR マウストラッキングパケット、カーソル位置報告、Kitty キー、Alt 括弧プレフィックスなど）や、捕捉されなかったマウス移動が生成する断片は厳密にフィルタされ、不正な文字として入力バーへ漏れ出すことはなくなりました。
- **Windows コンソール制御ハンドラーの起動時パニック** —— `PHANDLER_ROUTINE` コールバックが `bool` 戻り値を宣言していたため、`windows.NewCallback` が要求するポインタサイズ（`uintptr`）の結果と一致せず、登録する長滞留コマンド（`panda daemon`、`panda web`）が起動直後に "compileCallback: expected function with one uintptr-sized result" でパニックしていました。コールバックは `uintptr`（`1` 処理済み / `0` 未処理）を返すよう修正（#2）。
- **設定ファイルが無い状態でのパス解決** —— パス解決は `/etc` ではなくユーザーディレクトリへフォールバックし、設定の書き込み時は親ディレクトリを先に作成します。`node.card_path` がカードの場所として尊重されます。
- **カード編集が黙って無効化されなくなった** —— 既存 id の追加や存在しない id の削除はどこでもエラーになり、エンジンは変更のたびにカードを再読み込みするため即座に反映されます。
- **`ctrl+t` の後も、`exec` 中もホイールが機能する** —— 選択モードへ戻す際にオルタネートスクロールを再要求するようにし（Bubble Tea は DECSET 1007 を自ら発行しないため、フラグをモードに手で追随させる必要があります）、ホイールイベントはモード判定より前に処理されるため、長時間タスクの出力を読み返すのに PageUp は不要になりました。exec 実行中のクリックは引き続き無視されます。
- **TUI：再開した会話が再び表示される** —— ベアチャットを再び開くと、保存済みのターンがウェルカムバナーの後ろのスクロールバックに再生されます（直近 10 組まで。畳まれた件数は注記で表示）。
- **TUI：ウェルカムバナーはカーソル位置に出力** —— 高いターミナルで挨拶が下端まで余白で押し下げられ、入力行が視線の外に置かれることがなくなりました。
- **TUI：ホイールをターミナルに返す** —— マウスセルモーションを捕捉しなくなり、ホイール・スクロールバー・PageUp/PageDown がトランスクリプト自身のスクロールバックに届きます。クリック面はキーボード操作（y/n、Esc/Enter）を維持します。
- **Windows インストーラー：自動起動が実際の設定を読む** —— ログオンタスクが `--config`/`--card` をインストールプレフィックスの存在しないパスに固定しなくなり、`panda init` が書いたユーザーレベル設定を LaunchAgent / systemd と同じ順序で発見します。
- **Ask エンジンが空のスキルパスで起動可能に** —— `storage.skills_path` 未設定の設定ファイルでもエンジンの初期化が壊れなくなりました。スキルストアは空のまま開始するだけです（3f28486）。
- **SQLite が外部キー制約を有効化** —— ストアは `foreign_keys=ON` で開かれるようになり、削除済み親を参照する行が蓄積することはありません（7c5c643）。
- **TUI：オルタネートスクリーンでもバナー・ヒント・チャットを維持** —— オルタネートスクリーンへの移行時に ASCII ロゴ、ヒント行、会話ビューが欠落していました。3 点ともフルスクリーンレイアウトに戻っています（e2be53e）。
- **`/lang` が実際に UI 言語を切り替えるようになった** —— 選択したロケールが即座に UI 全体へ反映され、`config.yaml` に保存されるよう修正。
- **モデル注入後のメタデータ保持** —— 注入実行時のモデル名と通知イベントが正確に伝播されるよう修正。
- **キュー投入タスクが再びクロスデバイス・ルートする（CLI とボード）** — `panda task add` とボードの POST はキュー投入タスクすべてをノード全体の作業ディレクトリにピン留めしていた。v0.0.6 以降 forwardScheduled はピン留めされたタスクを定義上ローカル専用の作業とみなすため、当該能力を持たないノード上の `--requires pi.uptime` は、その能力を持つ peer に届く代わりに `route: no capability matches` で失敗していた——v0.0.5 が直したのとまさに同じ問題が、1 リリース後に知らぬ間に元に戻されていた。ピン留め自体は冗長（実行側は同じノード全体のデフォルトにフォールバックする）であり削除された。独自のディレクトリを持つタスク（パネルセッションの worktree）だけがローカルに留まる（今回のリリース）。
- **SSE 指紋キャッシュがロード失敗を伝播** — 失敗したストア走査の後ろに積まれた呼び出し元は、nil error の空値ではなく同じ error を受け取るようになった。ストアが故障し続けても、ロードした本人のストリームだけが落ちて他の全ストリームに偽の変更イベントを撒くことはなくなった（2026-09-02 レビュー P2）。
- **TUI：スラッシュコマンド1 つに入力ボックス1 つ** — exec 経路はコマンドをキューに入れる同じイベントループのターンでフレームを消すので、状態行と角丸ボックスがスクロールバックに第二の入力バーとして残らなくなった（6a77bf7）。
- **TUI ステージ計時、タスクカード整形、DeepSeek thinking パスバック** — 判定時間を実行ステージに課さなくなり、タスクカードの描画が揃い、思考モード会話のパスバックが 400 で失敗しなくなった（6d6e2e4）。
- **CLI テーブルが表示幅で整列** — `%-Ns` のパディングはバイトを数えるので、CJK 題名（1 文字2 カラム）や着色状態セルが後続の全カラムをずらしていた。テーブルは表示幅でパディングし、task id は曖昧でない場合短形式で表示、TUI レイアウト修正一式も同時に届く（8740c04）。
- **Darwin ビルドの自動コード署名** — ビルド時の ad-hoc 署名で、macOS が新しくビルドしたバイナリを初回実行時に SIGKILL しなくなった（3b86987）。
- **ドキュメント：OpenAI 互換の例から引退済み DeepSeek チャット・モデル別名を削除**（cbe52cb）。

### 破壊的変更

- **管理ツール `taskq_create` を削除** —— タスク生成はエントリモデルの専権（タスク JSON 出力、スケジューラが即時実行）か、ボード／外部ツールからのみとなりました。キューツール（`taskq_list/show/cancel/priority/move`）はバックログ管理用に残ります。`taskq_create` を呼ぶようスクリプト化していたエージェントは、タスクを出力するかボードを使うよう変更してください。

## [0.0.7] - 2026-08-31

使いやすさのリリース：ケイパビリティカード——スケジューラにこのノードができることを伝えるファイル——がデーモンの再起動なしで、あらゆる画面（CLI、REPL、TUI、Web コンソール）から編集可能に；2 台目のデバイスの追加は設定ファイルのパズルではなく製品フローに；すべてのタスク結果に人間が読めるサマリーが付くようになり、ユーザーは生の stdout の壁ではなく何が起きたかを見られる。

### 追加

- **全画面での構造化カード編集** — `panda card native add|remove`、`panda card agent add|remove|set`、`panda card manual add|remove`（構造化サブコマンド、エディタだけではない）；REPL と TUI では `/card` で同じ操作；Web コンソールには完全なカードエディタ（`/api/card` と agent/native/manual エンドポイント）。すべての書き込み経路は同一のバリデータ + `.bak` + アトミック書き込みパイプラインを通るため、不正な編集がカードを壊すことはない（1b8e2b7）。
- **デバイスペアリング** — `panda pair` が共有シークレットを生成し、新しいデバイスの参加手順を表示し、双方の設定に書き込む；`panda nodes add <addr>` はピアを追加して再起動なしで即時ダイヤル；Web コンソールの「デバイスを招待」CTA がノードページの実ペアリングフローに接続（763bff6, 5748cec）。
- **カードのホットリロード** — カード編集（どの画面からでも）が `ReloadCard` を発動：スケジューラが再読み込み・能力を再登録し、新カードを乗せたハートビートを全接続ピアにブロードキャストするため、変更はデーモン再起動なしで伝播（3d6feeb）。
- **Bubble Tea TUI** — `panda` は動作する tier-2 承認経路付きの Bubble Tea フロントエンドに入る（インライン承認カード、`y` で承認、`n` で `/approve` へ保留）；クラシック REPL は `PANDA_CLASSIC_REPL=1` で引き続き利用可能（06cca6a）。
- **LLM タスクサマリー** — すべてのインラインタスク（成功・失敗）の完了後、エンジンがエントリモデルを呼んで何が起きたかの人間が読めるサマリーを生成；サマリーは REPL・TUI・Web コンソールで生出力の前にレンダリングされ、ユーザーは「何をしたか + 主要な出力」（成功）または「なぜ失敗したか + 次の一手」（失敗）を見る。モデル失敗時は優雅に縮退——サマリーをスキップし生出力を表示（本リリース）。
- **Web：思考のストリーミングとタスク進捗** — モデルの推論が折り畳み可能な思考ブロックとしてチャットにストリーミング（03a4301）；タスクメッセージはペイロードだけでなく進捗と結果を表示（4ba931f）。
- **リモート tier-2 レジューム** — リモートノードへ委任された tier-2 タスクが承認された後、再実行は承認者のマシンではなく実行者（仕事が属する場所）で行われる（3d6feeb）。
- **常駐ゴルーチンの recover ガード** — 新設の `internal/guard` が長時間稼働するゴルーチンを包む：panic 時は完全なスタックを記録して制御されたシャットダウンを引き起こし、半死状態のプロセスを動かし続けない；バス接続ごとの読み取りループの panic はその接続だけを閉じる。
- **Windows でのグレースフルシャットダウン** — CTRL_CLOSE/LOGOFF/SHUTDOWN コンソールイベントが、unix の SIGTERM と同じ秩序あるシャットダウン経路を発動する（`SetConsoleCtrlHandler`、短いクリーンアップウィンドウ）。
- **Windows コンソールの色** — TERM 未設定（Windows コンソール）の場合、TUI パレットは TTY 出力で色を有効にする；`dumb` と `NO_COLOR` は引き続き優先。
- **`make build-darwin-amd64`** — Intel Mac 用ビルドターゲットを他のプラットフォーム別ターゲットに追加。
- **エージェント能力面とタスク単位ツールポリシー** — エージェントレジストリが各 CLI のネイティブ能力（skills、MCP、サブエージェント）を宣言し、アダプタごとのハードコードを廃止；エントリモデルはタスク単位のツールポリシー（`minimal` / `extended`）を要求してグローバルルーティングポリシーを上書きでき、高複雑度タスクはそのタスクだけエージェントの全能力面を解放できる。Claude Code のサブエージェント起動（Task ツール）は型付き `subagent` 進捗イベントとして現れ、スロットリングなしでタスクタイムラインに入る（本リリース）。
- **タスク種類別エージェントタイムアウト** — `timeouts.agent_by_kind` がタスク種類ごとにエージェントの壁時計バジェットを上書き（訓練タスクはクイック修正より長く走れる）；未列挙の種類は `timeouts.agent_s` に従い、タスクリースは常に実効バジェット以上に保たれる（bcbe1d2, e573c2e, 9fc2d04）。
- **サーキットブレーカー状態をハートビートで伝播** — エージェント CLI が失敗し続けるノードはハートビートでブレーカー状態を宣言し、ピアは壊れたエージェントにぶつかる前にルーティングを止める（bcbe1d2）。
- **エージェントセッションの再開と使用量記録** — 監督ラウンドは毎ラウンドのコールドスタートではなくエージェント自身の会話を再開（アダプタ線プロトコルに `session_id` + `resume` を追加）；アダプタは構造化トークン使用内訳を報告し `agent_usage` イベントとして記録（e8dc68b, 183bf6f, 1722144）。
- **委任深度の上限** — 同意情報がホップ制限と共に線に乗り、タスクは限られたホップ数までしか再委任できず、委任チェーンの無限成長は不可能に（ca5770e）。
- **キャンセルの確実な配信** — `task_cancel` は結果と同じ outbox に乗る：実行者が切断中に発行されたキャンセルは永続化され、再接続時に再配信され、失われない（dc4412a）。

### 修正

- **P0 セキュリティ指摘のクローズ** — plan_id/stage_id パストラバーサル（ステージ作業ディレクトリの `../../../../` による任意ディレクトリの読み取り+持ち出し）は ID 検証 + ルートプレフィックス表明でブロック；ピア切断後の結果配信は outbox に永続化され再接続時に再配信（レビュー P0-2）；TUI の割り込み/終了セマンティクスを修正し Ctrl+C が実際に終了するようになった（763bff6, 5129461）。
- **P1 セキュリティ強化** — デフォルトのリッスンアドレスを `0.0.0.0:7836` から `127.0.0.1:7836` に変更（デーモンはデフォルトで全インターフェースにバインドしない）；`context_fetch` はピアがタスクの委任チェーン内であることを要求；スーパーバイザー到達不能時は未検証結果を黙認せずタスクをレビューに退避（763bff6）。
- **エントリモデル：重複ユーザーターンの解消** — 厳格なプロバイダ（Anthropic 互換）は、セッション再生でユーザーターンが重複・浮遊した会話に 400 を返していた；正規化ステップが同一ロールの連続プレーンテキストターンを統合するようになった（8174e78）。
- **オーケストレーションのタイミングと Web メッセージ競合** — judge の実行時間は実行ステージに計上されなくなった（独立の `judge_start` トレースマーカー）；監督ループはラウンド結果の前に実行をトレースし、continue→continue 経路が再実行を隠さない；Web の楽観ターン状態は `chatstate.ts` に抽出され、エラー時は楽観バブルを削除しアシスタントの返信がユーザーメッセージ内に着地しない（97d5c62）。
- **キャンセルとエグゼキュータ受け入れの競合** — エグゼキュータの受け入れウィンドウ中に到着したキャンセルが破棄されていた；キャンセルはキューに入り、受け入れ完了後に適用される（a19b33b）。
- **Windows ゲートと相互ダイヤルハンドシェイクの決定論性** — クロスプラットフォーム CI ゲートが Windows で通るように；相互ダイヤルの tie-break は到着順に関係なく決定的（526c731）。
- **CI：並列ゲートジョブ** — ゲートワークフローは build/vet/test/typecheck を並列ジョブで実行し、レース検出器を必要なパッケージに限定し、Web コンソールの typecheck もゲート化（3f302f1）。
- **マイグレーションの相互排他** — スキーママイグレーションは `BEGIN IMMEDIATE` 下で実行し、トランザクション内で `user_version` を再確認するため、2 つのプロセスが同じデータベースを開いても各バージョンはちょうど 1 回だけ適用される；データベーススキーマより古いバイナリは静かに続行せず明確にエラーとなる。
- **Web：単一イベントバス** — コンソールは `Authorization` ヘッダーで認証する参照カウント付き SSE 接続を 1 本だけ保持（URL にトークンなし）、指数バックオフで自動再接続し、change と trace イベントを全購読者に分配する。
- **Web：セッションストリームの競合** — ストリーミング書き込みはセッションがアクティブな間のみ適用；ストリーム中にセッションを切り替えてもバブルが他のセッションに混入せず、切替時に古いトランスクリプトの読み込みを中断する。
- **Web：堅牢性とアクセシビリティ** — 再試行付きのトップレベルエラーバウンダリ；コマンドパレットと確認ダイアログのフォーカストラップ；キーボード操作可能なカンバンカード（Enter/Space、フォーカス可視）；タブ非表示時はシステムポーリングを一時停止し、進行中のポーリングをスキップ；安定したリストキー。
- **`panda skill --help` / `panda reminder --help`** — `--help` を未知の動詞として扱わず、使い方を表示して終了コード 0 で終了する。
- **CI：ゲートとインストーラの修復** — 失敗していた 4 本のゲートとインストーラパイプラインをすべて修復（7c418b0）。
- **CLI：折り畳まれた思考ブロックが、展開できないキーを表示しなくなった**（e772598）。
- **孤立フォワードと陳腐化したディレクトリ行** — 死んだピアが残した宙吊りの中継は救回されて完了し、ピアに支えられないディレクトリ行は永久滞留せず掃除される（32e4489）。
- **push タイムアウトは下流をキャンセル** — タイムアウトした push は下流の作業を放置せずキャンセルし、リトライバジェットは再起動を生き延びる（f7efb70）。

### 変更

- **デフォルトのリッスンアドレス** — デーモンはデフォルトで `0.0.0.0:7836` ではなく `127.0.0.1:7836` にバインドする。旧デフォルトに依存していた既存のデプロイは、`config.yaml` で `network.listen_addr` を明示的に設定するか `OPENPANDA_LISTEN_ADDR` を使う必要がある。
- **プラットフォーム対応のシステム設定ディレクトリ** — システムレベルのフォールバック設定ディレクトリは unix では引き続き `/etc/openpanda`、Windows では `%ProgramData%\OpenPanda`。
- **ストア初期化を一つの経路に統一** — デーモンと Web パネルは同じ関数でストアを開く（`cmd/panda/store.go`）；パネルが成果物プールディレクトリを見落とすことがなくなった。
- **Web パネル：イベントスキャンを接続数から分離** — タスク/ノード/リマインダーのフィンガープリントを 1 ポーリング間隔キャッシュし、購読者が増えてもスキャン負荷はほぼ一定。
- **アダプタごとのチューニング** — 残りのエージェントアダプタは汎用パスではなく、それぞれの CLI に特化した起動処理を得た（24df1c1）。

## [0.0.6] - 2026-08-27

クロスデバイス計算リリースの骨格：異なるマシンで異なるステップをこなす必要のあるリクエストが、ハードウェアのある場所で段階が走る一人前の plan になり、CLI と Web コンソールの両面に不足していたプレゼンテーション層——ask が収束する間のライブフィードバック、ブラウザでの本物の Markdown、日常使いに耐える入力エディタ——が備わった。

### 追加

- **Plan plane——段階が別マシンで走るパイプライン** — 段階はただのタスク（CAS 状態機械、lease、再試行、監督、レビュー停泊）なので、パイプラインはタスクが持つすべてを継承する。完了段階の作業ディレクトリはパックされてチャンク分割され、バス経由で次の段階を走らせるマシンへ運ばれる。入口は 2 つ：`panda plan example > train.yaml`、`panda plan run train.yaml [--dry-run]`、`panda plan show <id>`——あるいは `panda ask` ひとことで、モデルはリクエストがマシンを跨ぐ必要があるときにだけ plan を出す。段階は tier-2 同意を一切運ばない：不可逆段階は人が承認するまで review に停泊（c10b8af）。
- **宣言されたハードウェアによるルーティング** — `resource_profile` はハードフィルタ（`ledger.Fits`）で、スコアラーは空き容量 + キュー深さ + ティアで順位付けし、ハートビート鮮度で割り引く——同時に publish された 2 タスクは 2 台のマシンに分散する。入口プロンプトは各ノードの実ハードウェアを運び、モデルはルーティングフィルタを盲目ではなく埋める（c10b8af）。
- **`panda voice`** — ウェイクワード → ASR → 同じ入口パイプライン → TTS：キーボードのないデバイスのためのデスクペット入口（c10b8af）。
- **`panda card show | rescan | edit | set`** — 能力カードを操るコマンドファミリ：印刷（と出所ファイル）、ハードウェアと導入済み agent CLI の再スキャン（`rescan` は差分を印刷し、`--write` は適用して `.bak` を残し、手書きの決定は保持）、`$EDITOR` での編集、ヘッドレスなフィールド設定。`panda detect`、カードの rescan、パネルが同じ検出層（`internal/hwinfo`）を共有する（fdb56b8）。
- **CLI のプレゼンテーション層** — `internal/cliui`：一度だけ解決される単一パレットと、ライブステータス行（スピナー、動詞、経過秒、トークン数——後者 2 つは記録されていたのに一度も表示されていなかった）。パイプでは静的行へ退化する。行エディタは括弧付きペーストと複数行入力（貼り付けた複数行プロンプトは 1 回の ask で、履歴も 1 エントリとして呼び出せる）、Ctrl-R インクリメンタル履歴検索、打ち直したくない id の引数位置補完を学ぶ。未知コマンドには did-you-mean。`/help` は目的別グループでインライン印刷。最初の ask が動いた後に必要になるコマンド（`/cost`、`/model`、`/status`、`/doctor`、`/export`、`/clear`）と `@file` 添付、`!cmd` パススルーで、プロンプトから離れずに済む（c538ab6）。
- **Web チャット面の追いつき** — 手書き Markdown レンダラ（`innerHTML` ゼロ、ゆえにサニタイザ依存なし。29 の node テスト）が、返信のリテラルな `**bold**` と ``` フェンスを置き換える。ストリーミング中の主操作は停止ボタン（SSE リーダーは AbortSignal を受け取る）。読者が上へスクロールしたら自動スクロールは引き戻さない。Cmd+K パレットはサイドバーと同じナビゲーション語彙を共有。モバイルのスレッドドロワーが `display:none` を置き換える（c538ab6）。
- **ステータスページ** — `docs/status.md` が、動くもの・ビルドされただけのもの・欠けているものを、旗艦パイプラインの検証状態付きで記録する（76c5b69）。
- **失効したノード行を削除できる** — `panda nodes remove <id>` とオフラインノードカードの削除ボタンで、稼働中の peer に裏付けられないディレクトリ行（改名したマシン、変わったアイデンティティ、退役ノード）を落とす。ローカルノード自身の行とオンラインノードは拒否される——どちらも自分で再登録するので、「削除」は成功の言葉を着た無操作にしかならない。
- **リリースノート整備** — リリースワークフローがバージョンの CHANGELOG セクションとプラットフォーム別インストールコマンドを release 本文として公開し、セクション欠落時はビルドを失敗させる。0.0.5 の release ページはその標準で書き直され、本文は英語のみに。全 CHANGELOG の先頭にワンコマンドインストール（4e12779, c25a3cb, 98e10df, 600ffb3）。

### 修正

- **キュー投入タスクと plan 段階が実際にルーティングされる** — キューパスは「自分ができるなら自分がやる」ローカルショートサーキットを保持していた——ルーター本体はとっくに除去済みなのに、このパスはすべてのパネルタスクと plan 段階の通り道だった：ハードウェアフィルタは旗艦パイプラインが実際に走る場所で一度も走っていなかった（GPU 段階は Pi がその能力を持っていれば Pi に留まり、タスクの一斉投入は受けたノードに全部留まった）。決定はスケジューラに属する。空の ability リストは「誰も一致しない」ではなく「制約なし」を意味し、plan 段階はそれぞれがリソースキーを持ち、独立段階は互いに並走する（a5b792e）。
- **完了した実行の結果がフレームに収まる** — `task_result` 出力はバスフレームサイズにクランプされ、完了した実行の結果が提出者に届く——フレームを溢れて消える代わりに（c1310da）。
- **メモリのフェンスは内側から閉じられない** — `<memory_data>` フェンスは本体を包むだけで本体自身のタグに触れていなかった：リテラルな閉じタグを含むエントリはフェンスを早々に終わらせ、残りを指示として読ませていた——しかもメモリはモデル自身のツール、パネル、夢の昇格候補から書ける。内側のタグは中和され、テキストは監査のため可視のまま（3f18994）。
- **ノードが他人のマシンのハードウェアを語らなくなる** — どれも「プローブがあるべき場所にハードコード値」：デフォルトノード名は "macbook" で、`panda init` を走らせたことのないノードは作者のラップトップ名で名乗っていた。macOS/Windows にマシン ID の供給源はなく、改名は別ノードに見えた。Windows サンドボックスは PATHEXT/SYSTEMROOT/TEMP を剥奪していた——Windows 計算ノードが adapter を起動できなかったまさにその理由。`python3` は移植可能な名ではない（今は probe する、Windows は `py -3` 優先）。タイムアウトしたタスクは Windows で永遠にハングした（harness がプロセスツリーを殺せなかった。今は `taskkill /T`）。コマンドの存在しないネイティブ能力を広告するカードはルートを勝ち取って 127 で死んだ（ロード時に刈り取る）。サイズを読めない GPU は 0 を書き、その存在理由である仕事から除外された（「不明」は第三の状態に）。`deploy-pi.sh` はある開発者の LAN アドレスをデフォルトにしていた（今は必須）（fdb56b8）。
- **i18n 回帰の封じ込め** — 音声パス、ask/repl の plan 出力、会話要約、アンインストールエラー、`panda help` の 1 ヒントに固定されていた中国語が `internal/i18n` の 5 ロケールへ移された——ja/es/de ユーザーはこれらの面で中国語を見ていた（c538ab6）。
- **REPL が 1 秒未満で開き、peer ダイヤルのタイムアウトを待たない** — 対話起動はバナーの前に設定済み peer 全員へ**直列**にダイヤルし、それから着地を待っていた：オフラインの peer がダイヤラの満額 10 秒タイムアウトを最初のプロンプト前の空白として焼いていた。REPL、`panda session`、`panda voice` はバックグラウンドでダイヤルするようになり（長命セッションではオフライン peer は日常で、失敗が入力の途中に WARN 行を印字することもなくなった）、単発の `panda ask` は並行ダイヤルに——到達不能な peer が到達可能な peer を門番しない。

## [0.0.5] - 2026-08-25

3 デバイスラボパッチ：公開インストーラで導入し LAN で接続、エンドツーエンドで駆動した初の macOS + Orange Pi + Windows 実クラスタが浮き彫りにした問題——キュー投入タスクが発信ノードから一歩も出られない、tier-2 認可が委譲境界で失われる、ロックアウトされた agent CLI がルーティングを引き寄せ数分ハングする——を修正する 5 コミット。すべて実機で検証済み。

### 追加

- **`panda task add --requires`** — タスクに必要な能力を宣言（`--requires gpio:read`、カンマ区切り）。ローカルに該当能力のないキュー投入タスクは、それを持つデバイスへルーティングされる。`panda ask` が一貫して使うルートスケジューラ方式と同一（c4e1bc7）。

### 修正

- **キュー投入タスクのクロスデバイスルーティング** — `panda task add` と Web コンソールのタスクは発信ノードのみが認領・実行していた：能力が他デバイスにしかないタスクは即座に失敗（Mac で `pi.uptime` を提出すると `route: no capability matches`）。認領時にルートスケジューラへ問い合わせ、ローカルに一致がなければ有能な peer へ認領を付け替え（拒否ノードのループ保護、実行ノード死亡検出の lease 付き）、peer の結果が発信元の行を完成させる。ラボで 3 方向を実測：Mac→OrangePi、OrangePi→Mac、Windows→OrangePi すべて完了（c4e1bc7）。
- **Tier-2 認可が委譲を伴って伝播** — `--authorize` の同意は提出ノードに局所化していたため、peer へ委譲された agent タスクはユーザー承認済みでも実行ノードの防御層で弾かれていた。同意は認証済みバスで伝播し、実行ノードが尊重する：資格情報のない Orange Pi が Mac の claude へ認可済み coding タスクを提出すると、review で死ぬ代わりに完走する（c4e1bc7）。
- **ロックアウトされた agent CLI がルーティングを引き寄せない** — 能力カードは静的だが、インストール済み CLI は使えないことがある：ログイン状態なし・モデルキーなしの Windows の `claude.exe` がクラスタへ `agent:*` を広告し、ルーティングが coding タスクを送り、数分ハングしてからネットワークエラーで失敗していた。ローカルのフォールバックチェーンも hello で広告する能力サマリも可用性でゲートする——CLI が PATH 上にあり *かつ* モデルが到達可能（自身の資格情報または注入）。この Windows ノードのサマリは `win.sysinfo` のみを広告するようになった（2db530f）。
- **`panda web` がポート占有で死なない** — 2 回目の `/web`（または残留プロセス）は `bind: address already in use` を出し、token を手コピーさせていた。コンソールは近傍ポートへ前進しそを明示する。ブラウザは認証済みで開き（token は印刷されない）、稼働中の `/web` はログイン済みブラウザを開き直す。`--no-browser` は手動用に token 付き URL を従来通り印刷する（c4e1bc7）。
- **Peer hello が実バージョンを報告** — 3 つの hello パスがハードコードの `0.1.0-dev` を広告し、混在バージョンのクラスタで `panda nodes` の表示がすべて誤っていた。`version.Version` を報告する（ラボの 3 台すべて 0.0.5 を表示）（2db530f）。
- **解決済み設定ファイル横の能力カードが `./capabilities.yaml` に優先** — たまたま capabilities.yaml を含むディレクトリ（リポジトリのチェックアウト、他ノードのカード）から daemon を起動すると誤ったカードを黙って読んでいた。設定ファイルの横に init が書いたカードを優先し、`--card` が明示指定として最上位のまま（2db530f）。
- **Windows データディレクトリがインストールプレフィックスと衝突しない** — デフォルト状態ディレクトリ `%LOCALAPPDATA%\openpanda` とインストール先 `%LOCALAPPDATA%\OpenPanda` は大文字小文字を区別しない NTFS では同一ディレクトリ：SQLite ストア・メモリ・プロジェクトがインストールプレフィックス内部に置かれ、アンインストールが一掃していた。データディレクトリは `%LOCALAPPDATA%\openpanda-data` に。0.0.4 からの Windows ノードは新規ストアで起動（fc50721）。
- **インストーラが GitHub API レート制限と WinPS 5.1 の壊れた HTTP スタックを生き延びる** — `api.github.com` は未認証で IP あたり毎時 60 リクエスト。枯渇時、両インストーラとも `/releases/latest` の 302 リダイレクトで最新版を解決する。`install.ps1` は冒頭で TLS 1.2 を強制、バンドルの `curl.exe`（Windows 10 1803+）を優先し `Invoke-WebRequest` にフォールバック、タイムアウトを追加して壊れた WinINET プロキシがハングせず即失敗する。両方とも実 3 デバイス導入で実際に命中した（109b567）。
- **Homebrew tap プッシュの認証** — リリースワークフローの tap 更新ステップが job token 権限不足で `could not read Username` に失敗していた。プッシュ URL に token を埋め込む（6868a63）。

## [0.0.4] - 2026-08-25

> このセクションは要約版です。完全な内容は [CHANGELOG.md の 0.0.4 節](CHANGELOG.md) を参照してください。
>
> ハイライト：物理/VM ノード種別と安定したノード ID、ホスト単位の singleton daemon guard（`nodeidentity` パッケージ）、アダプタープロトコルの硬直化と契約テスト、`/api/self` + `/api/nodes` と Nodes Web ページ、3 ノード分散実験ツール、Homebrew/任意 cwd での SQLite 起動失敗（SQLITE_CANTOPEN 14）の根本修正。beta 以降：エントリーモデルの判定キャッシュ、階層化 system prompt、ゼロ設定 Web オンボーディング、共有アダプター harness、tier-2 承認 UX、インストーラー/アンインストーラーの整備、アップデーターの changelog ダイジェスト、1 問式 `panda init`、シナリオ別 FAQ。

### 追加

- ノード種別 `physical | vm` と安定した identity。VM は `node.identity` 必須。peer hello v2 で伝送、`employee_cache` v10 で既存行を `DEFAULT 'physical'` で埋め戻し。
- `nodeidentity.Acquire(kind, identity)`：Unix は `flock(2)`、Windows は `LockFileEx` で同一識別子の `panda daemon` 二重起動を防止。
- アダプタープロトコル統一（`{ok, result, exit_code}` + stderr 診断キャプチャ）。`tests/adapter_contract_test.py` 新設。
- `/api/self` + `/api/nodes`、web の Nodes タブ（running/last-seen テーブル）。
- 3 ノード実験スクリプト群と `docs/testing/distributed-lab-plan.md`。

### 修正

- **Homebrew / 任意 cwd での起動失敗（SQLITE_CANTOPEN 14）**：`config.Default()` をユーザーごとの `UserDataDir()` にアンカー、`config.Load()` で相対パスを YAML のあるディレクトリへ再配置、`storage.Open()` で DB 親ディレクトリを自動作成、`panelStore()` で REPL/web/queue 系入口も完全なディレクトリセットを作成。

### 変更

- `panda nodes` 出力に `Kind` カラム追加。

## [0.0.3] - 2026-08-23

### 追加

- **マルチエージェントアダプターレジストリ**——`internal/agents` は PANDA が委譲するすべての agent CLI の唯一の真実源（アダプタースクリプト、プローブバイナリ、インストールコマンド、ドキュメント URL）。`panda detect`、`panda agents`、Web 設定 API、commander の可用性プローブはすべてここから読み取るため、agent の追加は 1 箇所の変更で済む。
- **4 つの新しい agent アダプター**——Grok Build、DeepSeek Harness（`dsh`）、OpenClaw、Hermes が Codex、Claude Code、OpenCode に加わった。いずれも CLI を実行して `{ok, result, exit_code}` を返す小型のヘッドレス Python ブリッジ。
- **`panda agents`**——`list`（デフォルト）は PATH 上の各 agent をベストエフォートでバージョン検査する。`test <name>` は接続確認、`install|update <name>` はインストールコマンド + ドキュメントリンクを表示する。何もインストールされていない場合、全エージェントのインストールコマンドとダウンロード URL を一覧表示する。
- **Web 設定の agent 一覧**——設定ページの agent リストで、未インストールの agent ごとにインストールコマンドとダウンロードリンクを表示する（`/api/agents` が `install_hint` + `install_url` を返す）。
- **上位の完了判定（`superior task review`）**——agent 実行後、エントリモデルがタスクの成功基準に照らして結果を判定する（`entry.Supervise`、出力は `done`/`continue`）。`continue` 判定は後続の指示（残り作業 + 次のステップ）を agent チェーンに再委譲し、レビューが受理されるか上限ラウンド数（デフォルト 5）に達するまで繰り返す。
- **リスクによる終端ルーティング**——完了した可逆タスクは **done**（完了）へ。受理済みの不可逆（Tier-2）タスク——push・削除・不可逆な状態変更——は結果とともに **review**（承認待ち）で待機し、人間の承認を仰ぐ。レビューが何度も却下するタスクは `needs_followup` の印付きで **review** に留まる。レビュー判定イベントは Web のタスク詳細で再生される。
- **ワンラインインストーラー**——`scripts/install.sh`（POSIX）と `scripts/install.ps1`（PowerShell）が対応プラットフォームのリリースアーカイブをダウンロードし、SHA-256 を検証して、バイナリと agent アダプターをユーザー単位のプレフィックスに展開し、`panda` を `PATH` にリンクする。任意で自動起動サービス（ログイン時に `panda daemon`）を登録できる。macOS には Homebrew tap（`brew tap Xustalis/openpanda && brew install openpanda`）も提供する。
- **リリースパッケージング**——`scripts/package.sh`（および `make package`）が全対応プラットフォームをクロスコンパイルして `dist/panda-<version>-<os>-<arch>.tar.gz` / `.zip` と `checksums.txt` を生成し、GitHub Releases にそのまま利用できる。
- **セルフアップデート**——`panda web` と REPL の `/web` は実行中にリリースチャネルをバックグラウンドでチェックする。Web コンソールが更新をダウンロード・検証し、タスクキューがアイドルになると 1 クリックで適用する（バイナリの原子的置換、アダプター更新、再起動）。ダウンロード済み更新を破棄しても残留物は残らない。Windows では置換時に生じる `.old` サイドカーを次回起動時に掃除する。

### 修正

- **複数行の `--version` バナー**（例：Hermes）が 1 行の agent テーブルを汚す問題を解消——バージョン出力は CLI と Web 設定 API の両方で先頭行に切り詰められる。

## [0.0.2] - 2026-08-22

CLI ファーストのリリース：カーネル再設計（stage A–C）が完了——すべての Web 機能に CLI 対応物が揃い、REPL が製品の正玄関となり、CLI は会話メモリ・リアルタイムタスク報告・出力先ごとの Markdown レンダリングを獲得した。

### 追加

- **CLI コマンドファミリー**——すべての Web 機能に CLI の対応物：`panda session | task | memory | config | agents | project`、すべてパネルとサービス層を共有。`panda ask` はヘッドレス用途の `--output-format json|stream-json` を獲得（a4cba5f）。
- **リソース考慮のローカルタスクキュー**——`core.Submit` が非同期化：ドラッグ順 → 優先度 → FIFO で並べ、リソースロックレジストリと `MaxConcurrent` で門番。リソースが衝突しないタスクは詰まったキューを追い越す。タスクは `priority`/`seq`/`session_id`/`resource_keys` を獲得（SQLite v9）（0e8d850）。
- **REPL 会話メモリ**——24k 文字の予算でペア整列の退避（ユーザー発言がその回答と切り離されて再生されることはない）、`~/.local/state/openpanda/conversation.json` に永続化。`/new`、`/history`、`!!`、`panda ask --continue` に対応（f0a1b9f）。
- **帯域外タスク報告**——REPL のウォッチャーが任意のタスクの終状態到達時（ボード・Web コンソール・ピア委譲）に ✓/✗ を 1 行表示し、入力行は乱さない。インライン ask は二重通知しない（f0a1b9f）。
- **ライブタスクボード**——`panda queue --watch` と `/tasks watch` が 2 秒ごとにキューを再描画、状態着色。Ctrl-C はプロセスではなくビューを終了（f0a1b9f）。
- **`internal/mdtext`**——出力先ごとの Markdown レンダリング：カラー TTY は ANSI 強調、パイプと素のコンソールはプレーンテキスト、TTS 前は必ず除去。ストリーミング差分も同じ規則（e94f72f）。
- **エージェントのライブ進捗**——アダプターが stderr に NDJSON 進捗ノートを流し、スロットルされた `EvProgress` イベントとして記録。`panda task <id>` とパネルのタイムラインに実行中の様子が見える（93a453a）。
- **インジェクションポリシー**——`injection.model: auto|always|never`：エージェントネイティブの資格情報がデフォルトで優先。注入はすべてタスク出力で宣言され監査ログに記録（852b27e）。
- **コスト考慮ルーティング**——能力 × cost_tier を `preferred_agents` ボーナス付きでスコアリングし、次善の一致へフォールバック（852b27e）。
- **メモリ刷新**——設定可能な上限（`memory.limits`）、マニフェスト風の選択的注入による複数ファイル topics、低重みの dream 沈殿（852b27e）。
- **`internal/hwinfo`**——共有ハードウェア探査パッケージ。`panda detect` と新しい `GET /api/self` を支える（852b27e、1a97fd7）。
- **パネルのアプリ設定とメモリ topics**——`GET/PUT /api/settings/app` は検証付きポリシー保存。メモリ API はファイルごとの topics に対応。コンソールのメモリページ製品化・設定グループ化・5 言語 i18n 同期（1a97fd7）。
- **`panda init`**——対話式初回ブートストラップ。`config.yaml` + `capabilities.yaml` を書き、`config.ResolvePath` が解決順序（フラグ > 環境変数 > ユーザー設定 > システム既定）を統一（f5610fc）。
- **コンソールの仕上げ**——共有 `PageHeader`/`ErrorState`、グローバルトースト、破壊的操作の確認ダイアログ（45ee941）。
- **REPL の人間工学**——プロンプト下のスラッシュコマンドメニュー、純 ASCII の figlet バナー、TERM=linux の英語/ASCII 退化、`--card` 自動発見（`./capabilities.yaml` → `/etc/openpanda/capabilities.yaml`）（f0a1b9f）。
- **`scripts/deploy-pi.sh`**——1 コマンドで Orange Pi へデプロイ：クロスコンパイル、バイナリのアトミック置換、systemd インストール、ヘルスチェック（d7bc87f）。

### 修正

- **アダプターの全実行タイムアウト**——CLI が途中で固まると（パイプ開放・出力なし）読み取りループが永久ブロックし、タイムアウトは stdout EOF 後しか効かなかった。CLI を独立プロセスグループで起動し、ウォッチドッグスレッドが期限にツリー全体をキル（332f2d4）。
- **Anthropic ツール API 互換**——tool_use ブロックが常に `input` を携える（引数なしは空オブジェクト）。厳格な Anthropic 互換プロバイダ（DeepSeek /anthropic）は後続ターンを 400 で拒否していた。ドット付きツール名は `^[a-zA-Z0-9_-]+$` 満たすようアンダースコアに（93a453a）。
- **codex が非対話な親の下で初期化できない**（初回ターン前に状態 DB と PATH エイリアスの書き込みが EPERM）——`-s danger-full-access` で実行し、PANDA の外側サンドボックスで拘束（332f2d4）。
- **エージェント失敗の理由が空**——アダプターの診断が Stderr へミラーされ、`store.Fail` とタスク結果が実際のエラーを運ぶ（93a453a）。
- **相互ダイヤル再接続ストーム**——重複解消の敗者の最後の hello が到着コネクションではなくレジストリ接続で送られ、ピアの身元を束縛できず毎秒ダイヤルし直していた（実機で 15 分 869 回 → 現在 1 回）（93a453a）。
- **work_path がない**とコマンドバイナリを責める誤解を招く fork/exec ENOENT が出ていた——daemon が起動時に全ストレージルートを mkdir（f0a1b9f）。
- **末尾フラグが位置引数に黙って飲み込まれる**（`panda task <id> --config x` で設定喪失）——全サブコマンドがフラグを先頭へ繰り上げ（f0a1b9f）。
- **補完ループ**——`/e` が `/exit ` に吸着し、バックスペースで再発火（f0a1b9f）。
- **SQLite v9 マイグレーションが旧 DB でクラッシュ**——`tasks` テーブルがない場合は作成するよう修正（0e8d850）。
- **API エラーが輸送ノイズではなく指針として届く**——401/403 は `model.api_key`、404 は `base_url`/モデル名、持続する 429/5xx はレート制限、接続失敗はネットワーク確認を提案（df47725）。
- **ゲートと硬化**——`make measure` が存在しない設定を参照、gofmt のずれ、README の Go バージョン誤記、`.gitignore` の `.openpanda/` 落ち、サンプル設定の幻のピアによる警告のホットループ、パネルへの `securityHeaders` 追加（cacde7b）。

### 変更

- **回答規律**——エントリプロンプトは結論ファーストを要求。エージェントプロンプトは出力ライダーを携え、最終メッセージはやったことを報告し、実行の詳細は `panda task <id>` のイベントに残る（93a453a）。
- **ストリーミング耐性**——`streamWithRetry` は何も届いていない間だけ一時断（429/5xx/ネットワーク）をバックオフで再送。`deltaGuard` はタスク JSON をバブルに流さず、JSON 途中の切断を再送可能に保つ。疲弊したツールループは最後のツールなし呼び出しで収束。ツール実行は分類時のレジストリスナップショットを固定し、MCP ホットスイッチ中の「unknown tool」を防止（df47725）。
- **グループ化サイドバー**——折りたたみセクション（タスク / デバイスとエージェント / 個人 / システム）、エントリプロンプトは「指揮者」ペルソナに（f5610fc）。
- **パネルエンドポイントのテスト**——17 テストで監査の高リスク穴を埋める：セッション CRUD + 実際の git エンドツーエンド（HTTP で worktree 切り出し・diff・マージ）、モデルキーのマスキング（生シークレットは外に出ない）、MCP 起動失敗の 400、skills ライフサイクル、リマインダー CRUD、システムエンドポイント（ad884bf）。
- **対話コマンドの静かな設定読み込み**——対話面では slog の雑音を消し、daemon は完全なログを維持（f0a1b9f）。

### 破壊的変更

- **素の `panda` は対話 REPL を開く**（従来のヘッドレス daemon ではなく）、カーネルは明示的な `panda daemon` へ移動。systemd ユニット・LaunchAgent・Windows ランチャー・Makefile の実行ターゲットは更新済み——`panda` を直接呼ぶ配備は `panda daemon` に切り替えること（f0a1b9f）。

## [0.0.1] - 2026-08-19

初のオープンソースプレリリース：完全なカーネル機能一式（daemon、CLI、P2P 委譲、監査チェーン、マイグレーション、スケジューラ、SSE パネル、組み込み Web コンソール、対話 REPL、クロスプラットフォームのインストールライフサイクル）に加えアシスタント層（エージェント感覚、リマインダー、MCP、worktree チャット、カンバン）。全期間ゲート全緑：build / vet / 全テスト / `-race` / クロスコンパイル。

### 追加

- **カーネル基盤**——リースとクラッシュ復旧付きタスク状態機械、認証付き WebSocket ノードバス、能力ディレクトリ、OpenCode アダプター付きローカル実行パイプライン（Sprint 0–1：1be8f85..307e13a）。
- **P2P 委譲**——クロスノードのタスクルーティング、コンテキスト階層転送、段階的権限モデル（Tier 1 自動 / Tier 2 承認）、GPIO アクセス、DCPS スケジューリングスコア（3040e18、6324a87）。
- **防御チェーン**——scope ドリフト検出、リトライループ検出、破壊的コマンド表付きコマンド分類（590cacc、c647c96）。
- **Hermes メモリとスキル**——日次ノート、沈殿付き dreaming、プロジェクトメモリ、ロード可能スキル。`panda skill` が CLI からスキル承認を管理し、コンソールに skills ビューを搭載（9a41b3e、c36cad1）。
- **音声サイドカー**——ウェイクワード、STT、TTS、VAD（ハードウェアゲート付き）、`OPENPANDA_WAKE_KEYWORD` / `OPENPANDA_WAKE_MODEL` 上書き（84faf08）。
- **実機デプロイ**——Mac / Windows / Orange Pi の 3 ノード検証、scope ルーティング、ヘッドレスカーネル形態（0aa9f73、7f1f8bd）。
- **監査とマイグレーション**——`prev_hash` 監査チェーン、PRAGMA `user_version` の SQLite マイグレーション、スロー DoS 防護、MCP クライアントのハードタイムアウト（7582754）。
- **スケジューラメカニズム**——DCPS 加重スコアを TMB ハートビート新しさで割り引き（30 分半減期）。容量駆動 accept/decline、拒否後の自動再経路（歴史の拒否者を除外）（f454909、7385a89）。
- **単発 CLI パネルコマンド**——`panda status`、`panda queue`、そして `panda task | cancel | approve | reject | logs` は REPL に入らずにノードを検視しタスクを管理（307e13a）。
- **対話 REPL**——全パネル面をカバーするスラッシュコマンド（`/ask`、`/tasks`、`/approve`、`/nodes`、`/web`…）、5 言語 i18n、ask エンジンは任意（6119493）。
- **組み込み Web コンソール**——Vite + Preact + TypeScript で再構築し `go:embed` でバイナリに折り込み。キュー/詳細/ask/プロジェクト/ノードビュー、ライブ SSE、5 UI 言語（61cc519、c9768c1）。
- **パネル書き込み経路 + SSE**——共有 `askengine` による `POST /api/ask`、プロジェクト、ノード、キャンセル、ログ、`/api/events` 変更フィード（b4fb9f5）。
- **`panda web`**——ゼロ設定のループバックコンソール、一時トークンの自動ログイン URL（47517e3）。
- **`panda install` / `uninstall` / `doctor`**——クロスプラットフォームのライフサイクル：永続 PATH 登録、独立自己診断、confirm + zip バックアップ付きのホワイトリスト安全なアンインストール（86b9b9d）。
- **カンバンキューボード**——作成フォーム、優先度サイクル、列ごとのドラッグ並べ替え、インライン承認（da9c9e1）。
- **git worktree のチャットセッション**——ストリーミング返信、ライブ思考チェーン、ちょうど 1 回の概要折り返し（c36cad1）。
- **MCP 統合とホットリロード**——1 つの stdio サーバー、スワップ前に実際に起動して検証。ツールは即座に参加し再起動不要（c36cad1）。
- **スケジュールリマインダー**——`reminder.set` ツールでエージェントが自己スケジュール。Web Push と SSE カウントダウン、`panda reminder` CLI（c36cad1）。
- **エージェントの感覚**——`time.now` と `weather.get`：モデルに時計も窓もない（c36cad1）。
- **codex アダプターと可視性**——インストール済み CLI の探査と接続テスト、`panda detect` のハードウェアスキャン（c36cad1）。
- **`panda metrics [--csv]` と `panda audit verify [--task <id>]`**——委譲メトリクスのエクスポートと監査チェーン検証（7582754）。
- **`scripts/smoke-delegate`**——プロセス間委譲検証器：exit 0 はピア専用能力タスクがピアで done に到達したことを意味する。
- **オープンソース文書**——5 言語 README、マージゲート付き CONTRIBUTING、公開ロードマップ（51031eb）。

### 修正

- **相互ダイヤル接続フラップ**——同時ダイヤルが無限の ~1 秒接続/切断サイクルを生んだ。`ensurePeer` の決定的タイブレーク（辞書順に小さいノード ID が勝ち）で TCP は 1 本だけ残る（879b42d）。
- **ワイヤプロトコル認可の隙間**——result/decline/accept/context-ack が送信者を現在の実行者と検証、CAS ガードが TOCTOU を閉じる、`waiting_context` は常にリース、ローカル失敗はゾンビを残さない（9622538）。
- **コマンド分類のバイパス**——`env -S` の再帰分類、`php -r` スキャン、`find -exec` / `tar --checkpoint-action` / `git push/commit` は Tier 2 へ fail-closed（f5db449）。
- **プロセスグループ管理**——キャンセル時のツリー全体キル（Unix `Setpgid`、Windows `taskkill /T`）と 630 秒のアダプターハードタイムアウト（f5db449）。
- **メモリ注入チャネル**——Hermes/Projects/skills のアトミック書き込み、外部入力の `[ext]` 汚染、`<memory_data>` で囲んだデータ非指示の前書き（a742585）。
- **キャンセル伝播**——`task_cancel` が委譲チェーンに沿ってホップごとに実行ノードへ連鎖（574632a）。
- **トランザクション書き込み**——タスク状態 UPDATE と監査イベント INSERT が 1 トランザクションでコミット（c5d34d4）。
- **包括チェック（D1–D32）**——委譲孤児の終状態化、転送コピーのリース化、hello HMAC の 5 分窓、NetworkGuard の設定済みエンドポイント固定、サブプロセス出力の上限（1694b7d）。
- **新規クローンのコンソール白画面**——git 復元されたハッシュ付き `index.html` が無視されたアセットを指していた。コミット済みプレースホルダを安定化し、`make web` が実ビルドの着地を検査（ab87f90）。
- **未知のサブコマンドが常駐 daemon を起動**（`panda statsu`）——exit 2 と使用法を出すように（a742585）。
- **音声ウェイクの既定値**——バックエンドごとの実在する内蔵キーワード（`hey_jarvis` / `porcupine`）（4ea73bf）。
- **プレリリース監査修正**——`panda help` の実装、プロンプトと例から「PANDA」ブランド残滓の除去、`config.example.yaml` への `mcp:` と `model.api_type` の記載、死んだロードマップリンクの修正（2f001c0）。

### 変更

- **硬いメモリ壁**——個人メモリはワークスペース（worktree 固定）会話に決して注入されない。プロジェクトメモリは実行側の ContextPack 経由のみ（da9c9e1）。
- **エージェントアダプターが段階モデルに参加**——未宣言のエージェントは Tier 2 既定で、サブプロセス起動前に拒否（a4d2d9e）。
- **OpenAI ワイヤ形式**——`/v1/messages` 互換と OpenAI 互換の両エンドポイント、両経路でストリーミング（c36cad1）。
- **シークレットファイル硬化**——`api_key` / `shared_secret` / `panel_token` を含む設定は 0600 に自動 chmod、環境変数を推奨（6275fd4）。
- **パネルはループバック既定**（`127.0.0.1:7840`）、非ループバックは平文 HTTP を警告（a742585）。
- **ピア再接続が古い接続を置換**——同一 ID の新接続がレジストリへ入れ替わり、除去は接続 ID で一致（7911bbe）。
- **インタプリタ `-c` 分類はホワイトリスト制**——純粋出力と証明できるコードのみ Tier 1（f5db449）。
- **配備基線の成文化**——平文 `ws://` はループバック/Tailscale/信頼済み LAN のみ、公のインターネットは TLS + `wss://`（7582754）。

### 破壊的変更

- **プロジェクトを OpenPanda に改称**——モジュールパス `github.com/Xustalis/OpenPanda`、環境変数は `OPENPANDA_` プレフィックス、ユニット `openpanda.service` / `com.openpanda.node.plist`、既定 DB `openpanda.db`。CLI バイナリは短名 `panda` を維持（ac71bb1、6f2083e）。

## 見送り項目

意図的に先送りし、可視性のために記録：

- コンソールのキーボードショートカット（新規チャット、クイックタスク、ビュー切替）。
- アシスタントのブラウザコンパニオン画面。
- コンソール内の第一級 git ビュー（ブランチ状態、履歴、リモート）。
- コンソールからの worktree 管理（一覧/整理/検視）。
- ユーザー調整可能なアシスタントの性格と表示。
- Web 検索キャッシュによる繰り返し取得とレイテンシの削減。
- タスクごとの推論強度段階（低/中/高）。
