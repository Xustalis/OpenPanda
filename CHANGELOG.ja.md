# 更新履歴

[English](CHANGELOG.md) · [简体中文](CHANGELOG.zh-CN.md) · [日本語](CHANGELOG.ja.md) · [Español](CHANGELOG.es.md) · [Deutsch](CHANGELOG.de.md)

## プロジェクト概要

OpenPanda（**Open** **P**ersonal **A**daptive **N**ode-based **D**istributed **A**ssistant）は個人向けタスクオーケストレーションカーネル：各デバイスで 1 つの `panda` バイナリが走り、ノード同士は認証付き WebSocket バスで発見し合い、エントリモデルが各リクエストを直接回答または実行可能なタスク仕様へ変換し、スケジューラが各タスクを最適なデバイスとエージェントへルーティングする。CLI がカーネルの主インターフェース（素の `panda` で対話 REPL に入る）で、Web コンソールは同じストアとエンジンの上に立つ薄いシェル。

## バージョン規則

- バージョンは `MAJOR.MINOR.PATCH` に従う。プロジェクトは初期開発期（`0.0.x`）：パッチリリースには機能追加・バグ修正・そして例外的な破壊的変更が含まれ得る——後者は必ず**破壊的変更**カテゴリに記載する。
- リリースは `vX.Y.Z` のタグで切る。前のタグ以降の全コミットが新バージョンのセクションに属し、`[Unreleased]` は直近リリース以降の作業を集める。
- 各バージョンは 4 分類で記録する：**追加**（新機能）、**修正**（バグ修正）、**変更**（改善と調整）、**破壊的変更**（アップグレード時に対応が必要）。
- 各項目は変更内容とユーザーから見える影響を 1〜3 行で書き、必要に応じて導入コミットを付記して遡りやすくする。
- 英語版（CHANGELOG.md）が正。zh-CN / ja / es / de の翻訳はそのミラーで、リリース前後は一時的に遅れることがある。

## [Unreleased]

## [0.0.2] - 2026-08-22

CLI ファーストのリリース：カーネル再設計（stage A–C）が完了——すべての Web 機能に CLI 対応物が揃い、REPL が製品の正玄関となり、CLI は会話メモリ・リアルタイムタスク報告・出力先ごとの Markdown レンダリングを獲得した。

### 追加

- **CLI コマンドファミリー**——すべての Web 機能に CLI の対応物：`panda session | task | memory | config | agents | project`、すべてパネルとサービス層を共有（a4cba5f）。
- **リソース考慮のローカルタスクキュー**——`core.Submit` が非同期化：ドラッグ順 → 優先度 → FIFO で並べ、リソースロックレジストリと `MaxConcurrent` で門番。リソースが衝突しないタスクは詰まったキューを追い越す。タスクは `priority`/`seq`/`session_id`/`resource_keys` を獲得（SQLite v9）（0e8d850）。
- **REPL 会話メモリ**——24k 文字の予算でペア整列の退避（ユーザー発言がその回答と切り離されて再生されることはない）、`~/.local/state/openpanda/conversation.json` に永続化。`/new`、`/history`、`!!`、`panda ask --continue` に対応（f0a1b9f）。
- **帯域外タスク報告**——REPL のウォッチャーが任意のタスクの終状態到達時（ボード・Web コンソール・ピア委譲）に ✓/✗ を 1 行表示し、入力行は乱さない。インライン ask は二重通知しない（f0a1b9f）。
- **ライブタスクボード**——`panda queue --watch` と `/tasks watch` が 2 秒ごとにキューを再描画、状態着色。Ctrl-C はプロセスではなくビューを終了（f0a1b9f）。
- **`internal/mdtext`**——出力先ごとの Markdown レンダリング：カラー TTY は ANSI 強調、パイプと素のコンソールはプレーンテキスト、TTS 前は必ず除去。ストリーミング差分も同じ規則（e94f72f）。
- **エージェントのライブ進捗**——アダプターが stderr に NDJSON 進捗ノートを流し、スロットルされた `EvProgress` イベントとして記録。`panda task <id>` とパネルのタイムラインに実行中の様子が見える（93a453a）。
- **インジェクションポリシー**——`injection.model: auto|always|never`：エージェントネイティブの資格情報がデフォルトで優先。注入はすべてタスク出力で宣言され監査ログに記録（852b27e）。
- **コスト考慮ルーティング**——能力 × cost_tier を `preferred_agents` ボーナス付きでスコアリングし、次善の一致へフォールバック（852b27e）。
- **メモリ刷新**——設定可能な上限（`memory.limits`）、マニフェスト風の選択的注入による複数ファイル topics、低重みの dream 沈殿（852b27e）。
- **`internal/hwinfo`**——共有ハードウェア探査パッケージ。`panda detect` と新しい `GET /api/self` を支える（852b27e、1a97fd7）。
- **パネルのアプリ設定とメモリ topics**——`GET/PUT /api/settings/app` は検証付きポリシー保存。メモリ API はファイルごとの topics に対応。コンソールのメモリページ製品化・設定グループ化・5 言語 i18n 同期（1a97fd7）。
- **`panda init`**——対話式初回ブートストラップ。`config.yaml` + `capabilities.yaml` を書き、`config.ResolvePath` が解決順序（フラグ > 環境変数 > ユーザー設定 > システム既定）を統一（f5610fc）。
- **コンソールの仕上げ**——共有 `PageHeader`/`ErrorState`、グローバルトースト、破壊的操作の確認ダイアログ（45ee941）。
- **REPL の人間工学**——プロンプト下のスラッシュコマンドメニュー、純 ASCII の figlet バナー、TERM=linux の英語/ASCII 退化、`--card` 自動発見（`./capabilities.yaml` → `/etc/openpanda/capabilities.yaml`）（f0a1b9f）。
- **`scripts/deploy-pi.sh`**——1 コマンドで Orange Pi へデプロイ：クロスコンパイル、バイナリのアトミック置換、systemd インストール、ヘルスチェック（d7bc87f）。

### 修正

- **アダプターの全実行タイムアウト**——CLI が途中で固まると（パイプ開放・出力なし）読み取りループが永久ブロックし、タイムアウトは stdout EOF 後しか効かなかった。CLI を独立プロセスグループで起動し、ウォッチドッグスレッドが期限にツリー全体をキル（332f2d4）。
- **Anthropic ツール API 互換**——tool_use ブロックが常に `input` を携える（引数なしは空オブジェクト）。厳格な Anthropic 互換プロバイダ（DeepSeek /anthropic）は後続ターンを 400 で拒否していた。ドット付きツール名は `^[a-zA-Z0-9_-]+$` 満たすようアンダースコアに（93a453a）。
- **codex が非対話な親の下で初期化できない**（初回ターン前に状態 DB と PATH エイリアスの書き込みが EPERM）——`-s danger-full-access` で実行し、PANDA の外側サンドボックスで拘束（332f2d4）。
- **エージェント失敗の理由が空**——アダプターの診断が Stderr へミラーされ、`store.Fail` とタスク結果が実際のエラーを運ぶ（93a453a）。
- **相互ダイヤル再接続ストーム**——重複解消の敗者の最後の hello が到着コネクションではなくレジストリ接続で送られ、ピアの身元を束縛できず毎秒ダイヤルし直していた（実機で 15 分 869 回 → 現在 1 回）（93a453a）。
- **work_path がない**とコマンドバイナリを責める誤解を招く fork/exec ENOENT が出ていた——daemon が起動時に全ストレージルートを mkdir（f0a1b9f）。
- **末尾フラグが位置引数に黙って飲み込まれる**（`panda task <id> --config x` で設定喪失）——全サブコマンドがフラグを先頭へ繰り上げ（f0a1b9f）。
- **補完ループ**——`/e` が `/exit ` に吸着し、バックスペースで再発火（f0a1b9f）。
- **SQLite v9 マイグレーションが旧 DB でクラッシュ**——`tasks` テーブルがない場合は作成するよう修正（0e8d850）。
- **API エラーが輸送ノイズではなく指針として届く**——401/403 は `model.api_key`、404 は `base_url`/モデル名、持続する 429/5xx はレート制限、接続失敗はネットワーク確認を提案（df47725）。
- **ゲートと硬化**——`make measure` が存在しない設定を参照、gofmt のずれ、README の Go バージョン誤記、`.gitignore` の `.openpanda/` 落ち、サンプル設定の幻のピアによる警告のホットループ、パネルへの `securityHeaders` 追加（cacde7b）。

### 変更

- **回答規律**——エントリプロンプトは結論ファーストを要求。エージェントプロンプトは出力ライダーを携え、最終メッセージはやったことを報告し、実行の詳細は `panda task <id>` のイベントに残る（93a453a）。
- **ストリーミング耐性**——`streamWithRetry` は何も届いていない間だけ一時断（429/5xx/ネットワーク）をバックオフで再送。`deltaGuard` はタスク JSON をバブルに流さず、JSON 途中の切断を再送可能に保つ。疲弊したツールループは最後のツールなし呼び出しで収束。ツール実行は分類時のレジストリスナップショットを固定し、MCP ホットスイッチ中の「unknown tool」を防止（df47725）。
- **グループ化サイドバー**——折りたたみセクション（タスク / デバイスとエージェント / 個人 / システム）、エントリプロンプトは「指揮者」ペルソナに（f5610fc）。
- **パネルエンドポイントのテスト**——17 テストで監査の高リスク穴を埋める：セッション CRUD + 実際の git エンドツーエンド（HTTP で worktree 切り出し・diff・マージ）、モデルキーのマスキング（生シークレットは外に出ない）、MCP 起動失敗の 400、skills ライフサイクル、リマインダー CRUD、システムエンドポイント（ad884bf）。
- **対話コマンドの静かな設定読み込み**——対話面では slog の雑音を消し、daemon は完全なログを維持（f0a1b9f）。

### 破壊的変更

- **素の `panda` は対話 REPL を開く**（従来のヘッドレス daemon ではなく）、カーネルは明示的な `panda daemon` へ移動。systemd ユニット・LaunchAgent・Windows ランチャー・Makefile の実行ターゲットは更新済み——`panda` を直接呼ぶ配備は `panda daemon` に切り替えること（f0a1b9f）。

## [0.0.1] - 2026-08-19

初のオープンソースプレリリース：完全なカーネル機能一式（daemon、CLI、P2P 委譲、監査チェーン、マイグレーション、スケジューラ、SSE パネル、組み込み Web コンソール、対話 REPL、クロスプラットフォームのインストールライフサイクル）に加えアシスタント層（エージェント感覚、リマインダー、MCP、worktree チャット、カンバン）。全期間ゲート全緑：build / vet / 全テスト / `-race` / クロスコンパイル。

### 追加

- **カーネル基盤**——リースとクラッシュ復旧付きタスク状態機械、認証付き WebSocket ノードバス、能力ディレクトリ、OpenCode アダプター付きローカル実行パイプライン（Sprint 0–1：1be8f85..307e13a）。
- **P2P 委譲**——クロスノードのタスクルーティング、コンテキスト階層転送、段階的権限モデル（Tier 1 自動 / Tier 2 承認）、GPIO アクセス、DCPS スケジューリングスコア（3040e18、6324a87）。
- **防御チェーン**——scope ドリフト検出、リトライループ検出、破壊的コマンド表付きコマンド分類（590cacc、c647c96）。
- **Hermes メモリとスキル**——日次ノート、沈殿付き dreaming、プロジェクトメモリ、ロード可能スキル（9a41b3e）。
- **音声サイドカー**——ウェイクワード、STT、TTS、VAD（ハードウェアゲート付き）、`OPENPANDA_WAKE_KEYWORD` / `OPENPANDA_WAKE_MODEL` 上書き（84faf08）。
- **実機デプロイ**——Mac / Windows / Orange Pi の 3 ノード検証、scope ルーティング、ヘッドレスカーネル形態（0aa9f73、7f1f8bd）。
- **監査とマイグレーション**——`prev_hash` 監査チェーン、PRAGMA `user_version` の SQLite マイグレーション、スロー DoS 防護、MCP クライアントのハードタイムアウト（7582754）。
- **スケジューラ論文メカニズム**——DCPS 加重スコアを TMB ハートビート新しさで割り引き（30 分半減期）。容量駆動 accept/decline、拒否後の自動再経路（歴史の拒否者を除外）（f454909、7385a89）。
- **対話 REPL**——全パネル面をカバーするスラッシュコマンド（`/ask`、`/tasks`、`/approve`、`/nodes`、`/web`…）、5 言語 i18n、ask エンジンは任意（6119493）。
- **組み込み Web コンソール**——Vite + Preact + TypeScript で再構築し `go:embed` でバイナリに折り込み。キュー/詳細/ask/プロジェクト/ノードビュー、ライブ SSE、5 UI 言語（61cc519、c9768c1）。
- **パネル書き込み経路 + SSE**——共有 `askengine` による `POST /api/ask`、プロジェクト、ノード、キャンセル、ログ、`/api/events` 変更フィード（b4fb9f5）。
- **`panda web`**——ゼロ設定のループバックコンソール、一時トークンの自動ログイン URL（47517e3）。
- **`panda install` / `uninstall` / `doctor`**——クロスプラットフォームのライフサイクル：永続 PATH 登録、独立自己診断、confirm + zip バックアップ付きのホワイトリスト安全なアンインストール（86b9b9d）。
- **カンバンキューボード**——作成フォーム、優先度サイクル、列ごとのドラッグ並べ替え、インライン承認（da9c9e1）。
- **git worktree のチャットセッション**——ストリーミング返信、ライブ思考チェーン、ちょうど 1 回の概要折り返し（c36cad1）。
- **MCP 統合とホットリロード**——1 つの stdio サーバー、スワップ前に実際に起動して検証。ツールは即座に参加し再起動不要（c36cad1）。
- **スケジュールリマインダー**——`reminder.set` ツールでエージェントが自己スケジュール。Web Push と SSE カウントダウン、`panda reminder` CLI（c36cad1）。
- **エージェントの感覚**——`time.now` と `weather.get`：モデルに時計も窓もない（c36cad1）。
- **codex アダプターと可視性**——インストール済み CLI の探査と接続テスト、`panda detect` のハードウェアスキャン（c36cad1）。
- **`panda metrics [--csv]` と `panda audit verify [--task <id>]`**——委譲メトリクスのエクスポートと監査チェーン検証（7582754）。
- **`scripts/smoke-delegate`**——プロセス間委譲検証器：exit 0 はピア専用能力タスクがピアで done に到達したことを意味する。
- **オープンソース文書**——5 言語 README、マージゲート付き CONTRIBUTING、公開ロードマップ（51031eb）。

### 修正

- **相互ダイヤル接続フラップ**——同時ダイヤルが無限の ~1 秒接続/切断サイクルを生んだ。`ensurePeer` の決定的タイブレーク（辞書順に小さいノード ID が勝ち）で TCP は 1 本だけ残る（879b42d）。
- **ワイヤプロトコル認可の隙間**——result/decline/accept/context-ack が送信者を現在の実行者と検証、CAS ガードが TOCTOU を閉じる、`waiting_context` は常にリース、ローカル失敗はゾンビを残さない（9622538）。
- **コマンド分類のバイパス**——`env -S` の再帰分類、`php -r` スキャン、`find -exec` / `tar --checkpoint-action` / `git push/commit` は Tier 2 へ fail-closed（f5db449）。
- **プロセスグループ管理**——キャンセル時のツリー全体キル（Unix `Setpgid`、Windows `taskkill /T`）と 630 秒のアダプターハードタイムアウト（f5db449）。
- **メモリ注入チャネル**——Hermes/Projects/skills のアトミック書き込み、外部入力の `[ext]` 汚染、`<memory_data>` で囲んだデータ非指示の前書き（a742585）。
- **キャンセル伝播**——`task_cancel` が委譲チェーンに沿ってホップごとに実行ノードへ連鎖（574632a）。
- **トランザクション書き込み**——タスク状態 UPDATE と監査イベント INSERT が 1 トランザクションでコミット（c5d34d4）。
- **包括チェック（D1–D32）**——委譲孤児の終状態化、転送コピーのリース化、hello HMAC の 5 分窓、NetworkGuard の設定済みエンドポイント固定、サブプロセス出力の上限（1694b7d）。
- **新規クローンのコンソール白画面**——git 復元されたハッシュ付き `index.html` が無視されたアセットを指していた。コミット済みプレースホルダを安定化し、`make web` が実ビルドの着地を検査（ab87f90）。
- **未知のサブコマンドが常駐 daemon を起動**（`panda statsu`）——exit 2 と使用法を出すように（a742585）。
- **音声ウェイクの既定値**——バックエンドごとの実在する内蔵キーワード（`hey_jarvis` / `porcupine`）（4ea73bf）。
- **プレリリース監査修正**——`panda help` の実装、プロンプトと例から「PANDA」ブランド残滓の除去、`config.example.yaml` への `mcp:` と `model.api_type` の記載、死んだロードマップリンクの修正（2f001c0）。

### 変更

- **硬いメモリ壁**——個人メモリはワークスペース（worktree 固定）会話に決して注入されない。プロジェクトメモリは実行側の ContextPack 経由のみ（da9c9e1）。
- **エージェントアダプターが段階モデルに参加**——未宣言のエージェントは Tier 2 既定で、サブプロセス起動前に拒否（a4d2d9e）。
- **OpenAI ワイヤ形式**——`/v1/messages` 互換と OpenAI 互換の両エンドポイント、両経路でストリーミング（c36cad1）。
- **シークレットファイル硬化**——`api_key` / `shared_secret` / `panel_token` を含む設定は 0600 に自動 chmod、環境変数を推奨（6275fd4）。
- **パネルはループバック既定**（`127.0.0.1:7840`）、非ループバックは平文 HTTP を警告（a742585）。
- **ピア再接続が古い接続を置換**——同一 ID の新接続がレジストリへ入れ替わり、除去は接続 ID で一致（7911bbe）。
- **インタプリタ `-c` 分類はホワイトリスト制**——純粋出力と証明できるコードのみ Tier 1（f5db449）。
- **配備基線の成文化**——平文 `ws://` はループバック/Tailscale/信頼済み LAN のみ、公のインターネットは TLS + `wss://`（7582754）。

### 破壊的変更

- **プロジェクトを OpenPanda に改称**——モジュールパス `github.com/Xustalis/OpenPanda`、環境変数は `OPENPANDA_` プレフィックス、ユニット `openpanda.service` / `com.openpanda.node.plist`、既定 DB `openpanda.db`。CLI バイナリは短名 `panda` を維持（ac71bb1、6f2083e）。

## 見送り項目

意図的に先送りし、可視性のために記録：

- コンソールのキーボードショートカット（新規チャット、クイックタスク、ビュー切替）。
- アシスタントのブラウザコンパニオン画面。
- コンソール内の第一級 git ビュー（ブランチ状態、履歴、リモート）。
- コンソールからの worktree 管理（一覧/整理/検視）。
- ユーザー調整可能なアシスタントの性格と表示。
- Web 検索キャッシュによる繰り返し取得とレイテンシの削減。
- タスクごとの推論強度段階（低/中/高）。
