{
  "name": "openpanda-web",
  "private": true,
  "version": "0.0.8",
  "type": "module",
  "description": "OpenPanda web console — Vite + Preact + TypeScript, embedded into the panel binary at build time",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "preact": "^10.24.3"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.3",
    "@types/node": "^26.6.1",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
