# OpenPanda への貢献

OpenPanda をより良くすることに関心をお寄せいただき、ありがとうございます。このドキュメントでは、ツールチェーン、あらゆる変更が通過しなければならないエンジニアリングゲート、およびコードベースの可読性を保つための規約について説明します。

## 前提条件

| ツール  | バージョン | 用途                          |
| ------- | ---------- | ----------------------------- |
| Go      | ≥ 1.26.5 | カーネル、CLI、パネル、テスト |
| Node.js | ≥ 18       | Web コンソール（`webui/app`） |
| Python  | ≥ 3.10     | 音声サイドカー、Agent アダプタ |

## はじめに

```bash
git clone https://github.com/Xustalis/OpenPanda
cd openpanda
make run            # 設定例を使用してデーモンを起動
make web            # コンソールを webui/panel/dist にビルド（go:embed）
make build-webui    # コンソールを埋め込んだ単体パネル・サイドカーをビルド
```

デーモンはビルド時に Web コンソールを埋め込みます。`make web` を実行しなくてもプレースホルダページが埋め込まれるため Node なしで `go build` は動きますが、**UI 変更を出荷する前には必ず `make web` を実行**してください。

対話的な画面を簡単に体験するには：

```bash
panda repl          # スラッシュコマンド：/tasks /approve /projects /nodes /web …
```

## エンジニアリングゲート

プルリクエストは**すべての**ゲートがグリーンになった時のみマージされます。push する前にローカルで実行してください：

```bash
make gate           # build + vet + test + race（マージゲート）
gofmt -l internal/ cmd/ adapters/ webui/panel/   # 何も出力してはならない
cd webui/app && npm run typecheck                # Web コンソール変更時
```

CI は同じチェックを並行ジョブとして実行します——`lint`（gofmt + vet）、`test`、`race`（`make race-focused` により並行ホットスポットのパッケージに限定）、`web`（typecheck + node テスト + 埋め込みコンソールのビルド）、そしてクロスプラットフォームマトリクス——そのうえで、この完全な `make gate` が引き続きローカルの基準です。どちらもグリーンに保ってください。

- **`go test -race ./...` が必ず通ること**——カーネルは（ピアレジストリ・タスクストア・SSE ハブといった）並行システムです。レースディテクタの指摘は警告ではなく、リリースブロッカーです。
- コアモジュール（`internal/core`、`internal/scheduler`、`internal/storage`）は、現実的な範囲で**約 60% 以上のテストカバレッジ**を維持してください。バグ修正には、修正前に失敗するリグレッションテストを添えてください。
- 新しいワイヤープロトコルや委譲動作にはループバックテストを付けてください——パターンは `internal/core/dedup_test.go` と `scripts/smoke-delegate` を参照してください。

## コード規約

- **エラー**：`%w` でラップし、`errors.Is/As` で判定します。エラーを決して暗黙的に捨てず、同一関数内でログに記録し**かつ** return することも禁止します（どちらか一方）。
- **コメントは「何を」ではなく「なぜ」を書く**。半年後の読者が必要とするのは不変条件、トレードオフ、インシデント参照であり、コードの再説明ではありません。自明でない並行性の判断（ロック順、mutex の外で close する理由など）は、その場所に明記しなければなりません。
- **デッドコード禁止、投機的抽象化禁止**。3 行のよく似たコードは、時期尚早なインタフェースより優れます。使わなくなったコードはコメントアウトせず削除してください。
- **並行性**：すべての mutex が「何を守るか」を記述します。I/O や channel 送信をまたいでロックを保持しません。送信元が close を担当し、goroutine の所有者は spawn 箇所から一意に特定できるようにします。
- **セキュリティ**：失敗したら安全側に倒す（fail closed）。分類・認可・秘匿除去を行うものはすべて、デフォルトで制限的な分岐を取ってください（`internal/defense` の Tier モデル参照）。秘密情報を持つ新しい設定キーには 0600 chmod + 警告の仕組みを適用し、秘密をログに決して出さないでください。

## コミットスタイル

履歴に合わせた Conventional Commits：

```
feat(cli): 対話型 REPL — スラッシュコマンド、/web コンソール、5言語 i18n
fix(core): 確定的な相互ダイヤル重複排除 — 1秒の接続/切断フラップを解消
feat(web): フルコンソール — キュー/詳細/質問/プロジェクト/ノード + go:embed 単一バイナリ
```

`feat` / `fix` / `docs` / `refactor` / `chore` / `test` を型として使い、スコープはトップレベルレイアウトから選びます（`core`、`cli`、`web`、`scheduler`、`defense` …）。サブジェクト行は命令形で、`git log --oneline` で読める程度に具体的に記述します。

## Web コンソール（webui/app）

- スタック：Vite + Preact + TypeScript。実行時依存は Preact のみ。
- **ユーザーに見える文字列はすべて i18n を通す。** キー追加時は `webui/app/src/i18n/` の全ロケールに同時に追加してください（英語がフォールバックなので、「英語だけ先に追加して後で翻訳」は出荷しないでください）。
- CLI も同様：`internal/i18n/messages.go`。
- 言語追加：英語マップをコピー → 翻訳 → `internal/i18n/i18n.go` と `webui/app/src/i18n/index.ts` の両方にロケール登録 → README にリンクを追加。キーは grep 可能なまま保つこと——キー自体が識別子です。

## プルリクエスト

1. Fork し、`main` からブランチを切る（`feat/…`、`fix/…`）。
2. PR は小さく単一目的に保つ；ある機能とそのリファクタは 2 つの PR に分ける。
3. `CHANGELOG.md` の `[Unreleased]` を更新 —— Added / Changed / Fixed に整理する。
4. 5言語 README の更新義務は、ユーザーに見える CLI の挙動を変更したか、機能一覧に項目を追加した場合に限る；自分の追加した段落を他の 4 言語に翻訳するのは歓迎するがマージの足かせにはしない——メンテナが翻訳を同期します。
5. `make gate` グリーン → PR 作成 → レビュー。

## セキュリティ問題の報告

トランスポート認証、Tier モデル、秘匿除去層の脆弱性については**公開 Issue を立てないでください**。再現手順を添えて、リポジトリ設定のセキュリティ連絡先からメンテナに非公開で報告してください。改竄を前提に修正を検証できるよう、監査チェーン（`panda audit verify`）をまさにその用途で用意しています。

## ライセンス

貢献を頂いたコードは、プロジェクトの [MIT License](LICENSE) の下でリリースされることに同意したものとみなします。
