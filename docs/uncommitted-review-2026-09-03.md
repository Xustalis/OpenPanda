# 未提交变更排查报告（进行中）

> 生成日期：2026-09-03 · 状态：进行中 —— 边查边写，任何时刻中断都保留当前进度

## 0. 任务与方法

- 范围 A：未提交改动（`git status` 中 43 个修改文件 + 6 个新增路径，约 1782 行新增 / 231 行删除），主题是多模型注册表（`/model`）、TUI 菜单、WebUI 等。
- 范围 B：整个代码库的风险扫描。
- 优先级定义：
  - **P0**：会导致崩溃、数据丢失、安全漏洞、构建/测试失败、明显逻辑错误且影响主流程。
  - **P1**：行为缺陷但可绕过。
  - **P2**：代码质量、注释漂移、轻微问题。

## 1. 改动概览（初步）

- 变更主线：entry model 从单一 `model:` 升级为 `models:` 多模型注册表；新增 `internal/providers/`（内置 provider 目录）、`/model` 系列命令（`cmd/panda/modelcmd.go`）；config 支持模型列表热切换；TUI/REPL 接入；WebUI 会话页等。
- 待核实项见下文各节，逐项排查中……

## 2. 自动化基线验证（已完成）

| 检查 | 命令 | 结果 |
|---|---|---|
| Go 编译 | `go build ./...` | 通过 |
| 静态检查 | `go vet ./...` | 通过 |
| Go 单测 | `go test ./...` | 全部通过（无失败） |
| 前端类型 | `webui/app: npm run typecheck` (tsc --noEmit) | 通过 |
| 前端单测 | `webui/app: npm test` | 49/49 通过 |

结论：构建与测试基线健康，不存在"改完跑不起来"级别的 P0。
